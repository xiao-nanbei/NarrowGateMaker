[build-system]
requires = ["scikit-build-core>=0.10", "pybind11>=2.12"]
build-backend = "scikit_build_core.build"

[project]
name = "narrowgate-btcusdc-cpp"
version = "0.1.2.dev0"
description = "C++ acceleration hooks for NarrowGate BTCUSDC research and replay paths."
requires-python = ">=3.11"

[tool.scikit-build]
cmake.source-dir = "."
wheel.packages = []
