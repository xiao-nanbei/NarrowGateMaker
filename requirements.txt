# Legacy runtime/provider compatibility superset equivalent to the base package
# plus .[data,research,live,provider-cryptohft]. It intentionally excludes the
# pytest/Ruff developer tools. New source checkouts should use the narrower
# pyproject.toml extras documented in README.md.
#
# cryptohftdata is an optional authenticated third-party provider client.
# Installing this compatibility file grants neither provider access nor a
# license or research-eligibility status for any dataset.
requests>=2.28
cryptohftdata>=0.2.1
pandas>=2.0
numpy>=1.24
pyarrow>=14.0
lightgbm>=4.0
scikit-learn>=1.3
scipy>=1.10
pyyaml>=6.0
binance-futures-connector>=4.0
websocket-client>=1.6
pycryptodome>=3.19
zstandard>=0.23
