# Public Dry-Run Model Fixture

This is a synthetic, minimal LightGBM bundle used only to exercise the public deployment preflight and packaging path while ML is disabled. It is not trained on market data and grants no research, action, live, or baseline authority.

The fixture manifest binds every generated model, metadata file, and P3 example by SHA256. A real deployment must use its separately governed owner model bundle and deployment evidence.
