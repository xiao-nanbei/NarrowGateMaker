# F05 Fill Quality, Toxicity, And Quote EV

Last materially modified: 2026-08-16

> Publication note: `${NARROWGATE_*}` values and deployment-epoch names are logical locators. Owner-side data and machine artifacts are in the private evidence store and are not distributed with this repository unless a repository-relative link is provided. See the [public/private documentation contract](../../../docs/public_private_documentation_contract.md).

Documentation boundary: this README and the unit's tracked `docs/` are public. Owner-only artifact locators, unpublished evidence indexes, and private research context are resolved through this unit's ignored local `private/` catalog and are not distributed with the public repository. See the [public/private research layout](../../PRIVATE_EVIDENCE.md).

Status: BUY fill-selection action and shadow logging remain OFF. The separate SELL Boolean cooldown policy `causal_multichannel_window_boolean_cooldown_owner_policy_v1` is active live under `owner_risk_accepted_promotion`; its research hard gates remain failed. Re-enabling the BUY selector requires a new full-path comparison against the now-current active baseline. The immutable 40-day prefix records `-144.251748 USDC` terminal MTM, `-147.466348 USDC` closed-campaign value, and 17,118 fills; the pooled predecessor 50-day diagnostic records `-165.566079 USDC`, `-168.530979 USDC`, and 20,147 fills. This denominator is native-derived top-20/100ms rather than strict raw-native queue replay. Strict-native, latency-calibrated evidence remains mandatory for research-supported action promotion; the active cooldown is an explicit owner exception and must not be relabeled as a hard-gate pass.

The filled-only classifier and one-shot soft-widen implementation remain research code, but no pre-correction PnL result can authorize, close, or rank them on the current stack. A new study must estimate direct assignment-to-terminal incremental USDC using decision-visible inputs and the same live-held BER clock in both arms.

The current project-level PnL state is `decision_visible_fill_quality_alpha_missing`. A new feasibility identity may use the existing Development evidence with q90 frozen OFF identically in all arms; q90 terminal-risk-set repair and accumulation of new external receive-time dates are not foreground prerequisites. The target must be decision-visible post-fill net value with an exact lifecycle identity. Rows without that identity are excluded, and a fallback mid is forbidden.

The canonical prediction output is `expected_maker_markout_bps_per_opportunity_30s`. It is expected maker-signed markout in bps per quote opportunity, not USDC EV. `ev_30s` is a read-only compatibility alias for historical consumers.

The root files own Quote-EV modeling and training; `audit/` owns order-level denominators, nulls, toxicity, and selection scoring. The production scorer adapter remains under `strategy/`. Shared dependencies: D, R, S.

The Development-only successor [`decision_visible_negative_fill_value_evidence_m0_v1_1`](docs/decision_visible_negative_fill_value_evidence_m0_v1_1_development_20260729.md) used F10's native, hash-frozen first-add decision-to-terminal artifact, one decision per campaign, direct USDC/decision targets, expanding chronological OOF, and separate BUY/SELL evidence. Neither side improved on the campaign-state nuisance model with a positive familywise lower bound, and the predicted high-risk groups' mean-value intervals crossed zero. The identity is closed on Development; Validation and sealed holdout remain unread.

This does not negate the older fill-toxicity and maker-markout evidence. It shows that the currently frozen decision-visible local microstructure fields do not reliably identify which first exposure-increasing add fill will have negative decision-to-campaign-terminal value. Existing fill-conditioned toxicity or `expected_maker_markout_bps_per_opportunity_30s` outputs cannot be substituted for this target and cannot authorize an F09 action.

The earlier F10 live diagnostic localized a hypothesis-generating SELL opener question, but the later paired quote-coordinate audit withdrew the large BUY/SELL opener edge gap because 31 rows mixed a fallback mid with exact lifecycle values. Both sides still show negative fresh maker value. The corrected Development-only [`sell_first_fill_conditional_value_feasibility_v3`](docs/sell_first_fill_conditional_value_feasibility_v3_development_20260730.md) tested that question using exact native first-opener lifecycles, submit-time causal features, and a fully formal target-day plus previous-natural-day L2 identity.

Grade-A SELL first-opener value was negative on average, with a simultaneous interval fully below zero. The local model nevertheless failed to improve the past-only intercept baseline and did not isolate a selectively worse subset; its predicted high-risk group was less negative at the point estimate. The local prediction branch is closed on Development. Validation and sealed holdout remain unread, and no F09 action or live change is authorized.

The conditional-P3 joint-quote branch now records both governance paths. The immutable hard gate remains failed: its overlap has 28 days, three OOF folds, 282 paired buckets, and one fill in the sparsest side-role-action cell versus the frozen `30 / 4 / 30` requirements. The owner separately accepted that support for an outcome-informed Development continuation without rewriting the hard-gate result.

The owner-path [`conditional_p3_joint_quote_sparse_value_diagnostic_v1`](docs/conditional_p3_joint_quote_sparse_value_diagnostic_v1_development_20260804.md) reconstructed 269/282 complete terminal-overlay buckets, so value coverage was 95.39%; widespread missing value data was not the primary blocker. It evaluated 126 OOF buckets across 13 days and three chronological folds. No non-baseline action passed the past-only simultaneous economic screen, so the rule selected baseline for all 126 OOF buckets. The strongest early signal was about `3.7e-5 USDC/bucket`, still below half of the frozen `1e-4` economic threshold; later-fold intervals crossed zero. The failure is economic resolution and chronological stability.

This closes the current F05 sparse-value quote selector, not conditional P3 probability prediction. v4.1 is retained only as an offline F02 artifact; it is not a live or shadow input. Operational P3 v2 remains unchanged. The owner path stops in F05 because it produced no executable candidate; a 13-arm regenerated full-path replay is therefore not authorized. Development economic outcomes were read, while Validation and sealed holdout remain unread.

The dual-path labels describe possible future evidence classes, not equal current evidence. A normal passing route may eventually receive `research_supported_promotion`. A genuinely new owner successor could retain `owner_risk_accepted_promotion` only after independent positive full-path economics, execution parity, and safety gates. This branch has neither route to F09. The exact scope is frozen in [`conditional_p3_quote_mapping_status_closure_v1`](docs/conditional_p3_quote_mapping_status_closure_v1_20260804.md).

The source-aware [`multiscale_ema_add_wait_incremental_value_source_aware_v1_2`](docs/multiscale_ema_add_wait_incremental_value_v1_2_development_20260809.md) successor used both 2025 and 2026 data without granting provider data economic authority. Its unsupervised 2025 encoder consumed all 112,090,884 admitted side-specific 100 ms BBO rows across 66 days. Direct ADD-minus-WAIT value heads were trained only inside native 2026 exact-lifecycle chronological folds. The complete native panel contains 320 labels; the frozen 24-day outer-fold union contains 192 OOF rows, 96 per side.

Neither side passed the incremental prediction gate. BUY was worse than M0 on both frozen loss metrics with both 95% intervals below zero. SELL had positive point improvements, but both intervals crossed zero. Validation and sealed holdout remain unread, and no F09, action, or live permission exists. This closes the EMA ADD-vs-WAIT identity, not every possible trend signal, and does not establish the current 85-second cooldown as an optimal constant.

The original deployment-oriented [`multiscale_ema_boolean_cooldown_duration_policy_v1`](docs/multiscale_ema_boolean_cooldown_duration_policy_v1_development_20260810.md) then tested the separate state-to-duration question requested after v1.2. It materialized the complete 40-day native census of 8,600 opportunities across eight frozen duration arms per side, producing 68,800 C++ full-path fork rows. After joint censoring, 8,429 opportunities entered side-specific nested chronological training over all 45 EMA pairs and 360 frozen predicates. The 2025 provider panel was used only for unsupervised predicate normalization; all duration-value labels came from native 2026 lifecycle replay. Its deployment-strength LCB fallback ran before untouched outer OOF, so its 0% non-control action rate is a conservative abstention result rather than a test of non-baseline rule transport.

The [`exploratory OOF successor`](docs/multiscale_ema_boolean_cooldown_duration_policy_exploratory_oof_v1_development_20260810.md) then forced the best support-valid non-baseline rule into every outer fold and applied deployment gates only after all OOF rows were scored. BUY changed the duration on 76.99% of rows but produced -0.001511 USDC/campaign-weight with a -0.004711 lower bound. SELL changed 85.64% of rows but produced -0.003952 with a -0.018341 lower bound. Both side gates failed. No learned-policy full-path, Validation, holdout, F09 registration, or live deployment is authorized. This closes the frozen Boolean-duration candidate set, not all possible cooldown state models, and does not identify 85 seconds as optimal.

The new exploratory successor [`causal_multichannel_window_boolean_cooldown_duration_v2`](docs/causal_multichannel_window_boolean_cooldown_duration_v2_design_20260810.md) keeps those v1 results immutable and changes the information and execution identity. It freezes a completed 100ms causal source grid, explicit feature-ready cutoff, three-valued Boolean logic, mandatory action-magnitude and campaign context, and separate BBO, individual-trade, and depth EMA channels. The old eight-duration vocabulary is retained so feature changes are not confounded with action-grid reselection.

The outcome-blind execution pipeline is now implemented and hash-bound. It includes the atomic `CooldownAssignmentSnapshotV2`, raw-native M2 extraction, D-1/D/D+1 support, target-day parent stop with D+1 child washout, POSIX copy-on-write eight-arm forks, separate 2025 provider-book and official-trade predicate materialization, fold-local M0 fitting, and a self-contained nested chronological OOF admission. The frozen 50-day calendar yields 41 formal full-support days: 33 from the historical 40-day prefix and 8 from the added 10-day late diagnostic; the other 9 days remain a separate reduced-support identity.

Execution amendment v3 binds the formal opportunity identity to partial-fill ordinal and quantity, stores and rehashes the complete assignment snapshot, and cross-checks source, execution, clock, order, side, role, quantity, and duration identities across the opportunity and all arms. It also separates statistical OOF gates from effective deployment gates. R0 is reproduction-only; M0, M1, and M2 are selected through frozen paired incremental gates rather than an unrestricted best-of-four search. A continuous-state comparator remains required before any unified Boolean policy can be frozen. Execution amendment v4 now implements that diagnostic on raw, unquantized source fields with capacity selected only inside chronological inner folds. It remains unrun on formal economics and can block a weaker Boolean policy, but can never replace the Boolean identity or grant action/live authority.

Execution amendment v5 closes three identity gaps before formal materialization: strict-native warmup admission is propagated from the raw D-1 source rather than inferred from the UTC cutoff; cooldown deadline ownership is a bounded M0 input (`none` or `existing_same_side_lineage`); and outer OOF deployment support is counted only on campaigns and days that actually receive a non-control duration. It also permits book/trade literals in the same clause only when they share the frozen feature-ready cutoff, and keeps causal impact, absorption, and ownership-attributed depletion/refill explicitly outside the current reduced M2 claim.

Execution amendment v6 binds the frozen 41/9 denominator to the existing outcome-blind native sequence audit and replaces calendar-adjacency coalescing with overlap-only D-1/D/D+1 source segmentation. All 41 formal targets have strict D and D+1 sequence support. The formal source union is eight segments, 57 unique UTC days, and 1,368 hours; target start must be snapshot-seeded and all strict source-counter deltas through D+1 must be zero. A D-1 warmup gap may recover only through a snapshot before the target boundary.

Execution amendment v7 aligns the strict-label consumer with the v3 target receipt emitted by that source plan. Historical v2 segment and target receipts fail closed; immutable validated native-hour cache files remain reusable.

The strict-native 48-opportunity benchmark is durably admitted on `${NARROWGATE_PRIVATE_EVIDENCE_ROOT}`. All 48 eight-arm bundles completed; 22 were strict-exact and 26 failed closed per opportunity on queue ambiguity or invalidation. It remains engineering evidence only because it predates the hardened opportunity identity. The outcome-blind 2025 predicate artifact also completed all 134 book/trade parts, and the raw source union admitted 8/8 segments, 57 unique source days, 1,368 hours, and 41/41 target receipts with every frozen sequence and timestamp counter at zero.

That source admission did not make historical strict economics identifiable. Public trade rows provide millisecond timestamps while the book stream contains sub-millisecond ordering. Same-millisecond trade/book precedence cannot be recovered from the retained inputs, and inventing a tie-break would change queue seeds and fills. Execution amendments v8/v9 and the [`strict-native failure receipt`](docs/causal_multichannel_window_boolean_cooldown_duration_v2_strict_native_formal_execution_failure_20260811.md) therefore fail the 41-day formal label path closed. No reusable strict economic panel was produced or read; admitted raw source bytes remain reusable.

A separately frozen owner successor tested the question that can be answered with the existing modeled-queue Development panel. The historical [`Development report`](docs/causal_multichannel_window_boolean_cooldown_duration_v2_owner_modeled_queue_v1_development_20260812.md) must now be read together with its [`interpretation errata`](docs/causal_multichannel_window_boolean_cooldown_duration_v2_owner_modeled_queue_v1_interpretation_errata_20260812.md). The panel contains 40 days, 8,600 opportunities, and 68,800 executed duration arms. The historical `120,400` count is a dense side-action union matrix slot count, not an executed-arm count. 2025 data supplied outcome-blind predicate support only. M2 used its frozen 33-day common-support subset; missing feature or action outcomes were not imputed.

These panel counts are not OOF test denominators. R0/M0/M1 use a 40-day label panel with 28 actual outer-OOF test days. M2 uses a 33-day common-support label panel with 21 actual outer-OOF test days. The added 10-day panel and pooled 50-day panel were not run or imputed. Any successor must emit these three objects separately: eligible label panel, outer-OOF test dates, and final full-path execution denominator.

All 14 independent absolute Boolean cells failed. Non-control rules did enter the outer folds and materially changed durations, so this is not the old pre-OOF baseline-abstention bug. Both M0 absolute gates also failed, so `supported_sides=[]` remains the correct no-advance decision for this limited owner one-shot branch.

The frozen feature-family hierarchy itself was not implemented: the admitted report did not produce paired M1-minus-M0 or M2-minus-M1 evidence. Large feature blocks were reduced to 32 outcome-blind predicates, and all 56 outer policies contained one rule; no ordered multi-rule policy was tested. The interval was a UTC-day cluster sandwich normal interval, not a cluster bootstrap or a 14-cell simultaneous band. Consequently this result cannot close the original multichannel high-order Boolean architecture or any repeated-policy full-path PnL question.

No unified policy is currently eligible under this identity, so its downstream stages were not run. That resource decision is narrower than a structural closure of the research question. Validation and sealed holdout remain unread; research support, owner action authority, and live authority are false.

The distinct, outcome-informed [`persistent-policy v3 design`](docs/causal_multichannel_window_boolean_cooldown_persistent_policy_v3_design_20260812.md) has now been executed on the owner modeled-queue lane. It implemented the paired M0/M1/M2 hierarchy, broader ordered rules, a fold-level distribution audit, and shared UTC-day wild max-t inference. Both sides failed the M0 research gate, so v3 has no research-supported side and does not repair the predecessor's evidence status.

The owner then froze a non-research-supported SELL M2 diagnostic policy while BUY remained `CONTROL_85N`. In repeated-policy full-path replay, the pooled 50-day point estimate improved terminal MTM by `+11.372165 USDC` and closed-campaign value by `+9.747065 USDC`, with 96.04% fill retention and better tail/inventory points. Both primary intervals crossed zero and only 24/50 days improved, so the 50-day hard gate failed. The owner authorized only an unchanged-policy restart-aware confirmation.

The corrected 71-day calendar-continuous replay then improved terminal MTM by `+16.877254 USDC` and closed-campaign value by `+16.895254 USDC`. Fill retention was 97.66%, q10/CVaR and maximum inventory improved, but both primary lower bounds remained negative and only 26/71 dates improved. The independent historical scorecard therefore remains `historical_economics_audited_not_scoreable_no_authority`. The full result and the maintenance-bridge correction are in the [`owner full-path and continuous report`](docs/causal_multichannel_window_boolean_cooldown_owner_restart_aware_continuous_v1_20260812.md).

After that scorecard was frozen, the owner explicitly accepted the unresolved statistical, strict-queue, and transport risk and authorized the unchanged SELL policy for active live use. It is now enabled as `causal_multichannel_window_boolean_cooldown_owner_policy_v1` under the permanent `owner_risk_accepted_promotion` label. BUY remains `CONTROL_85N`, reducing quotes are unchanged, and unsupported or warming state falls back to the predecessor cooldown. This does not retroactively pass the research hard gates. The active scope, hashes, preflight, rollback identity, and current permissions are in the [`owner active release`](docs/causal_multichannel_window_boolean_cooldown_owner_active_release_v1_20260812.md).

The active policy's interpretation is now narrowed by the [`owner-policy interpretation errata`](docs/causal_multichannel_window_boolean_cooldown_owner_policy_v1_interpretation_errata_20260813.md). Historical OOF evidence belongs to fold-specific learning-algorithm policies, not the exact final refit artifact. The compiled policy was sourced from the cumulative M2 panel but uses only M0 campaign context and mid-EMA predicates; it uses no trade/depth feature. Its 4s/16s predicate is a three-valued feature-readiness guard, while campaign age and 16s/256s cross recency form the economic branches. Exact private policy and predicate-bundle bytes now match both the public research interpretation and the production evaluator over all 54 side-by-state cases, with zero discrepancies; this is implementation parity only. None of these interpretation checks alters active bytes or owner authority.

The prospective [`full-multiscale successor v1`](docs/causal_multichannel_window_boolean_cooldown_full_multiscale_successor_v1_spec_20260813.md) was withdrawn before deployment with zero rows and no economic read; no companion, observer, writer, journal, feature dump, or candidate telemetry is retained. Its independent [`offline successor`](docs/causal_multichannel_window_boolean_cooldown_full_multiscale_successor_offline_v1_spec_20260813.md) asks whether the full ten-half-life, 45-pair EMA bank, directional cross state, higher-order Boolean rules, and genuine trade/depth features add sequential campaign value over the exact active owner policy. The frozen first 30 family-specific historical Development targets passed the outcome-blind source gate, and the v2-v4 mechanics lineage established and preserved the exact ordered 3,516-opportunity denominator while fail-closed amendments corrected predicate projection, provider fold scope, exact-owner B0, owner-action provenance, and symmetric fill tracing. The [`fill-trace rebinding v4 admission`](docs/causal_multichannel_window_boolean_cooldown_full_multiscale_successor_offline_v1_fill_trace_rebinding_admission_v4_20260814.md) remains immutable historical evidence.

Formal-v13 passed preflight and its fixed one-day gate at 81/81 exact-owner no-op paths, but the nested OOF run was deliberately stopped because the predecessor adapter replayed every opportunity-duration arm from day start. The [`formal-v13 interruption amendment`](docs/causal_multichannel_window_boolean_cooldown_full_multiscale_successor_offline_v1_formal_v13_nested_oof_interruption_and_shared_prefix_replay_amendment_20260814.md) records ten running progress receipts and zero admitted cache entries, labels, candidates, outer-test results, scorecards, or economic evidence. The corrected resumable POSIX copy-on-write executor then rebuilt all 30 dates; the [`shared-prefix rebinding v5 admission`](docs/causal_multichannel_window_boolean_cooldown_full_multiscale_successor_offline_v1_shared_prefix_rebinding_admission_v5_20260814.md) confirms that the four state/action tables remain byte-identical to v4, all 3,516 opportunity IDs keep the same order, and only the portable binding path differs in replay inputs. Formal-v14 subsequently passed preflight and independently reproduced the 81-opportunity one-day gate, then began outer-train one-shot generation without an admitted candidate, outer-test result, scorecard, or final economic report. The frozen [`formal-v15 semantic fold-reuse amendment`](docs/causal_multichannel_window_boolean_cooldown_full_multiscale_successor_offline_v1_formal_v15_semantic_fold_reuse_execution_amendment_20260814.md) preserves separate fold receipts while allowing exact side/day replay-input frames to be rebound across expanding folds; an outcome-blind audit found 5,361 of 8,215 outer-train opportunity occurrences reusable and leaves 2,854 first-or-changed opportunities to compute. After v15 obtained its clean tag, fresh manifest, passing preflight, and 81/81 fixed one-day gate with eight arm workers, formal-v14 was deliberately interrupted. The [`formal-v14 interruption and v15 replacement admission`](docs/causal_multichannel_window_boolean_cooldown_full_multiscale_successor_offline_v1_formal_v14_interruption_v15_replacement_admission_20260814.md) records 271 atomic v14 cache shards but zero returned label batches, candidates, outer-test results, scorecards, or formal economic conclusions; v15 may not reuse those shards. Validation and sealed holdout remain unread, and action/live authority remains false.

Formal-v15 then completed the ten-day `outer1 / BUY` one-shot batch with 809 opportunities and 6,472 arms before failing closed while combining two `outer1.inner1 / BUY` repeated-policy day rows. One day contained a `consecutive_units_count::6` category and the other did not; the old concatenation converted that structural zero into `NaN`, which the formal count validator correctly rejected. The [`formal-v15 failure and formal-v16 amendment`](docs/causal_multichannel_window_boolean_cooldown_full_multiscale_successor_offline_v1_formal_v15_sequential_count_failure_v16_amendment_20260815.md) records twelve complete atomic cache entries but no returned inner result, candidate, outer test, scorecard, final report, or economic conclusion. Formal-v16 freezes a computation-only correction that adds typed zero only for structurally absent category columns while retaining fail-closed rejection of emitted missing, negative, or fractional counts; it requires a new clean tag, manifest, cache identity, preflight, and one-day gate before nested OOF can restart.

The [`formal-v16 replacement gate`](docs/causal_multichannel_window_boolean_cooldown_full_multiscale_successor_offline_v1_formal_v16_replacement_gate_admission_20260815.md) has now passed. Its clean tag and fresh manifest preserve the 30-day, 3,516-opportunity denominator and exact owner B0; preflight reports zero missing canonical fields, and the fixed 2026-06-27 gate reproduced 81/81 exact-owner no-op paths, 81 complete washouts, and zero censoring with eight arm workers. Formal-v16 is eligible only for a fresh nested OOF run under its new cache identity. It has no candidate, outer-test result, scorecard, final report, action authority, or live authority, and it may not reuse formal-v15 cache entries.

Formal-v16 subsequently completed the complete `outer1 / BUY` sequence: ten one-shot day entries, 25 inner-OOF day-policy entries, and all 65 outer-OOF day-policy entries for 13 candidate or comparator identities. It then failed closed before generating any `outer2` label because the correct washout purge removed all 58 BUY rows from the final nominal training day, while the adapter incorrectly required purged rows to retain every nominal day. The [`formal-v16 failure and formal-v17 amendment`](docs/causal_multichannel_window_boolean_cooldown_full_multiscale_successor_offline_v1_formal_v16_purged_day_scope_failure_v17_amendment_20260815.md) records 100 complete atomic receipts but no complete nested result, scorecard, formal report, or promotion conclusion. Formal-v17 permits only a provider-owned non-empty subset of nominal outer-train days after purge; all sequential evaluation paths retain exact requested-day coverage and v16 cache reuse remains forbidden.

The [`formal-v17 replacement gate`](docs/causal_multichannel_window_boolean_cooldown_full_multiscale_successor_offline_v1_formal_v17_replacement_gate_admission_20260815.md) passed. Its clean tag and fresh manifest preserve the 30-day, 3,516-opportunity denominator and exact owner B0; preflight reports zero missing canonical fields, and the fixed 2026-06-27 gate reproduced 81/81 exact-owner no-op paths, 81 complete washouts, and zero censoring with eight arm workers. Formal-v17 began its fresh nested OOF under that cache identity, but the owner has authorized an atomic efficiency switch only after a separately frozen accelerated successor passes its complete zero-economic preflight. Until that switch is admitted, v17 remains unchanged; it has no complete nested result, scorecard, final report, action authority, or live authority, and its partial strategy-dependent caches cannot seed the successor.

The execution-only [`offline executor acceleration v1`](docs/causal_multichannel_window_boolean_cooldown_full_multiscale_successor_offline_executor_acceleration_v1_design_20260816.md) provides a dependency-ready global `policy x day` pool capped at ten worker tokens, candidate-independent exact-B0 daily path reuse, and an immutable read-only mmap cache for daily trades, BBO, L2, derived state, and frozen model overlays. The formal manifest now binds those settings explicitly and requires a zero-economic walk over both sides, four outer folds, twelve inner folds, every fold-day slot, the complete candidate ladder, and the continuous comparator before economics can start; 170 combined Python contract and acceleration tests pass. An isolated C++ build passed 97 synthetic and existing parity tests, including explicit-BBO fill-price, cash, terminal-MTM, lineage, and checkpoint equality, but its accepted scope remains `synthetic_full_replay_smoke`; generic BUY/SELL, dynamic M0/M1/M2, authoritative support clocks, per-fill cancellation, assignment receipts, campaign accounting, and restart-chain parity remain formal blockers. Python remains authoritative, formal-v17 does not use this acceleration identity, and no economic, action, live, Validation, or sealed-holdout authority is created.

Formal-v18 exposed that acceleration v1 reached only the later sequential stage: one-shot training labels still ran several full pandas day parents, each with its own nested shared-prefix scheduling, so the nominal ten-worker contract did not govern the dominant workload and the new mmap was not opened there. The owner stopped v18 after 509 opportunity and 4,072 arm progress receipts, before any label batch returned or any candidate, outer-test result, scorecard, or formal conclusion existed. The [`offline executor acceleration v2`](docs/causal_multichannel_window_boolean_cooldown_full_multiscale_successor_offline_executor_acceleration_v2_design_20260816.md) corrects that execution identity with one active day parent, one global nine-arm pool, two bounded coordination supervisors, mandatory read-only mmap input for one-shot jobs, and a two-worker cold-materialization cap. The formula is one parent plus nine arm compute slots equals ten CPU tokens; v18 strategy-dependent caches are ineligible for reuse. Formal-v19 may start only from a fresh clean tag and manifest after the all-fold zero-economic walk and one-day exact-owner gate pass. Validation, sealed holdout, action, live, and EC2 state remain unchanged.

All exact OOF, 50-day, 71-day, daily, policy, scorecard, and deployment bytes are owned by the F05 private evidence catalog. Public prose exposes only stable artifact IDs, SHA256 identities, aggregate conclusions, and availability. Validation and sealed holdout remain unread; research-supported action/live authority remains false while owner-risk-accepted live authority is true.
