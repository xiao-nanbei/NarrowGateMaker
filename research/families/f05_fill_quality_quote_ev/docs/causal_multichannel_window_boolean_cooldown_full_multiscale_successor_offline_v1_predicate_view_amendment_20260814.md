# F05 Offline Full-Multiscale Predicate-View Amendment

Last materially modified: 2026-08-14

Status: `implemented_local_pre_economic_clean_binding_required`.

## Boundary

This amendment changes only the offline formal execution path for `causal_multichannel_window_boolean_cooldown_full_multiscale_successor_offline_v1`. It does not change the active owner policy, live runtime, EC2 configuration, lifecycle writer, action authority, or live authority. Validation and sealed holdout remain unread.

## Formal V6 Stop

The formal-v6 execution manifest and its mechanics preflight were valid for their then-current clean tag, but the subsequent `run` stopped before one-shot label generation or repeated-policy economics. The loader exposed primitive three-valued EMA states and continuous fields without first applying the frozen 2025 outcome-blind predicate bundle, while the fixed B1/B2/B3 policies requested three legacy compiled predicate names. No `formal_result.json`, PnL report, candidate action result, or economic label artifact was created. Formal-v6 is retained as a pre-economic implementation diagnostic and cannot be resumed after source changes.

## Corrected Predicate View

The admitted 3,516-row mechanics panel remains byte-identical and outcome-blind. A new offline-only predicate view now hash-validates the active predicate bundle and all four side-by-clock artifacts, requires every artifact reference day to be in 2025, applies the frozen transforms to the canonical panel, and reconstructs the three exact current-policy predicates from their bound inputs. The same module materializes only the selected predicates from each repeated-policy assignment snapshot, so training-panel and sequential-evaluator tri-state semantics share one implementation and one bundle identity.

The primitive panel contains 5,249 predicate columns after index and side columns are excluded. The bound 2025 transforms plus the three exact current-policy predicates produce 27,003 three-valued columns. BUY contributes 2,021 opportunities and SELL contributes 1,495, preserving the admitted 3,516-opportunity denominator.

## Candidate Separation

`E1_FULL_EMA_BANK` contains the 45 mid-EMA ordering predicates, one for every frozen fast/slow pair. `E2_DIRECTIONAL_EMA` contains 990 outcome-blind mid-EMA predicates covering every pair and every frozen direction, recency, persistence, distance, normalized-distance, slope, curvature, convergence, and expansion semantic. The two current-policy 16-second cross predicates are excluded from E1/E2 and remain available only to the fixed B2/B3 controls. `E3_HIGHER_ORDER_BOOLEAN` adds the campaign-age context predicate, for 991 columns. `M2_TRUE_INCREMENTAL` is cumulative E3 plus all non-mid local market predicates, for 26,911 columns, and requires 20,520 genuine trade/depth incremental predicates. The continuous comparator retains 8,700 raw continuous fields.

These counts describe candidate eligibility, not independent information dimensions and not a claim that every candidate will be selected. Inner-fold feature pooling, policy selection, and unknown-action handling remain governed by the frozen nested chronological contract.

## Verification

The offline predicate-view, backend, adapter, nested-OOF, final-refit, repeated-policy, confirmation, mechanics, source, and orchestration regression suite passed 246 tests. Dedicated tests prove panel/snapshot predicate equality and fail-closed behavior for bundle drift, artifact drift, malformed predicate values, malformed pre-expanded bindings, current-policy predicate leakage into E2, and fold-policy identity disagreement between executed and learning artifacts. Ruff and `git diff --check` pass on the changed surfaces.

## Next Gate

Formal economics remains locked until these exact public bytes are committed, annotated-tagged, and used to create a new immutable formal execution manifest. That fresh manifest must independently revalidate the source, panel, folds, exact owner hashes, predicate bundle, expanded candidate census, and mechanics preflight. Only a new preflight with status `formal_offline_replay_mechanics_ready` may begin nested OOF. The old formal-v6 manifest cannot authorize or cache any later run.

No economic outcomes, Validation, or sealed holdout were read while producing this amendment. Research, action, and live authority remain false for the successor.
