# F05 Offline Formal V24 Execution Design

Last materially modified: 2026-08-16

Status: `formal_v24_implemented_local_pre_admission`.

## Question

Formal-v24 asks only whether the locally verified post-v23 runtime-scope correction can enter a new clean execution identity without changing the frozen research question, and whether the all-panel zero-economic builder gate now rejects an unusable formal C++ runtime before any panel row is read.

## Owner Authorization

The owner explicitly authorized continuation after the immutable formal-v23 failure. This authorization permits a new execution-only identity; it does not authorize changes to data, candidates, durations, estimand, statistics, the active owner policy, EC2, Validation, sealed holdout, action, or live state.

## Immutable Research Identity

Formal-v24 preserves the exact formal-v23 30-day source manifest, 3,516 mechanics opportunities and ordering, exact current-owner B0 policy and predicate bundle, eight side-specific duration arms, candidate ladder, four-by-three chronological folds, sequential repeated-policy estimand, `action_alpha_v1` statistics, and false action/live permissions.

The v23-to-v24 invariance receipt must compare source, panel, days, opportunities, folds, B0 artifacts, duration vocabulary, candidate ladder, estimand, statistics, evidence boundaries, and every research-semantic source byte. The only permitted changes are versioned execution and receipt identities, the already committed runtime-scope allowlist correction, the builder runtime-instantiation gate, rebuilt C++ extension bytes, the clean commit and annotated tag, and newly generated immutable receipts.

## Runtime Scope

The formal C++ runtime continues to declare `real_day_all_arm_full_replay_v23`. That string identifies the previously tested runtime semantics, not the formal execution attempt number. Formal-v24 must not mint another runtime scope merely to mirror the execution version.

## Admission Gates

Before reading any panel row, the all-panel builder preflight must construct the exact formal runtime configuration, instantiate `F05RepeatedBooleanCooldownRuntime`, and require `parity_qualified=true`. It must then validate exactly 3,516 target rows with zero economic evaluator calls.

After the builder receipt, the first admitted real opportunity must run all eight legal side-specific duration arms through Python shared-prefix authority and C++ direct replay with zero digest mismatches. Only then may the first full qualification day run every opportunity and all eight legal arms.

The quick and full locksteps may compute path state only for implementation parity. They may not persist, expose, or select on economic values. Their receipts must bind the clean commit, annotated tag, formal execution manifest, v23-to-v24 invariance receipt, source and panel manifests, exact B0 artifacts, observation tape, C++ extension, and execution-source hashes.

## Engine Boundary

C++ authority remains limited to qualified outer-train one-shot label generation. Outer-test B0-versus-candidate evaluation remains Python-authoritative sequential repeated-policy replay. One-shot effects may not be summed into policy PnL, and formal-v24 grants no C++ outer-test, action, live, or deployment authority.

## Stop Rule

Failure of the 3,516-row builder walk, first-opportunity eight-arm gate, full qualification day, manifest or hash validation, or formal preflight permanently closes formal-v24. No replacement execution identity may be created automatically. Nested OOF may begin only after every pre-economic gate passes.

## Forbidden Operations

Formal-v24 must not deploy or modify EC2, create a shadow or companion, modify the active owner policy, read Validation or sealed holdout, change dates or folds, alter the candidate ladder, tune durations or model complexity, change the estimand or statistics, or grant action/live authority.
