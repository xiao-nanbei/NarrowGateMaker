# F05 Full-Multiscale Successor Execution-Attempt Normalization

Last materially modified: 2026-08-21

Status: terminology and SHA-governance correction; historical evidence is unchanged.

Evidence availability: SHA256 values below are identity metadata, not download links. Repository-relative links identify files available in this repository; exact manifests, caches, receipts, and results named without public links are retained in the private evidence store and are not distributed with the public repository.

Question: did the succession of F05 `formal-v13` through `formal-v27` labels represent fifteen different research questions?

Result: no. They were implementation and execution attempts under the same frozen research identity, `causal_multichannel_window_boolean_cooldown_full_multiscale_successor_offline_v1`. The sample, exact owner baseline, candidate ladder, folds, estimand, and statistical contract did not change across ordinary scheduler, cache, schema, C++ parity, mmap-lifetime, and resume repairs. Those labels are now explicitly classified as historical execution-attempt aliases, not research versions.

Evidence boundary: this correction does not rewrite or delete any historical commit, annotated tag, manifest, cache receipt, failure receipt, or result. It changes only how the lineage is interpreted and how future attempts are named. Exact private artifacts remain in the private evidence store and are not distributed with the public repository.

## Correct Identity Model

The one research identity is the frozen offline successor v1. Its historical `formal-v13` through `formal-v27` labels identify source/runtime attempts. A failed attempt remains immutable and economically unusable according to its own receipt. A repaired source must receive a new SHA-bound attempt manifest, but it remains the same research identity unless the sample, baseline/candidate contract, folds, estimand, or statistics change.

The accepted BUY component is the separately materialized immutable formal-v24 cache component bound to source commit `8b863a49987b97c88ae8d70ec6605f081c988575` and manifest canonical SHA256 `2021a70f2f15f4fff82240cdc494556413da0fc24d369be00fd60628bcf3395a`. The accepted SELL component is the repaired same-research-line run bound to source commit `18c305145d93606a1c1a4794d0f81d367b4c9715` and manifest canonical SHA256 `da4a55c4f870f64230493415d240769c2f1d346edd02731d573a8eab55177df8`. Their identity-only composition and result hashes are recorded in the [formal component receipt](causal_multichannel_window_boolean_cooldown_full_multiscale_successor_offline_formal_components_development_20260821.json).

This two-component closeout is a historical exception caused by the original run failure after BUY completed. It performs no pooled training, economic re-estimation, or cross-execution strategy-cache reuse. Future formal studies must stabilize the complete executor before freezing evidence and should produce one admitted attempt manifest for the run being interpreted.

## Future Naming Rule

Future code repairs use an `attempt-*` id or timestamp under the unchanged research identity. They must complete representative single-day output, an all-fold zero-economic walk, concurrency and cache durability, regression and implementation parity, and a complete output smoke before clean commit, annotated tag, and run manifest creation. The final receipt then binds result SHA256 values to that pre-run manifest.

Only a change to sample, baseline/candidates, folds, estimand, or statistical contract creates a new research identity. The project-wide executable rule is documented in the [formal execution-attempt contract](../../../shared/experiment_governance/docs/formal_execution_attempt_and_evidence_freeze_contract_v1_20260821.md).
