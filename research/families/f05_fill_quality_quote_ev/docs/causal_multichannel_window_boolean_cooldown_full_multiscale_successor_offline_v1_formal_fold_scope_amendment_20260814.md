# F05 Offline Full-Multiscale Formal Fold-Scope Amendment

Last materially modified: 2026-08-14

Status: `implemented_local_pre_economic_clean_binding_required`.

## Boundary

This amendment changes only the offline formal label-request path for `causal_multichannel_window_boolean_cooldown_full_multiscale_successor_offline_v1`. It does not change the active owner policy, live runtime, EC2 configuration, lifecycle handling, candidate ladder, statistical contract, action authority, or live authority. Validation and sealed holdout remain unread.

## Formal V7 Stop

Formal-v7 was clean-tag bound and passed its then-current mechanics preflight, but its first outer-train label request stopped before any replay arm, label, cache, candidate action, or economic result was generated. The canonical label provider selected the admitted outer-train rows but did not add the ephemeral formal fields `outer_fold_id` and `fold_row_role`; the replay adapter correctly refused to continue without them. The durable formal-v7 directory contains only the execution manifest and preflight. It contains no `formal_result.json` and cannot be resumed after this source amendment.

Formal-v7 therefore remains a pre-economic implementation diagnostic. Its stop does not say anything about candidate value, OOF performance, side support, or promotion eligibility.

## Corrected Scope Ownership

The admitted mechanics panel remains byte-identical and contains no formal fold-scope columns. Only `CanonicalFoldScopedLabelProvider`, after validating the immutable label request and selecting its exact row identities, may copy those rows and add `outer_fold_id=<bound request fold>` plus `fold_row_role=outer_train`. Pre-injected scope in the admitted panel is rejected, an empty fold identity is rejected, and the source frame is never mutated.

The replay adapter independently requires both scope fields, requires the only role to be `outer_train`, requires the only fold identity to equal the signed label request, and still validates the selected row sequence and row SHA. Missing scope now raises an explicit contract error rather than a raw dataframe exception. Outer-test, mixed-role, wrong-fold, caller-injected, or missing-scope inputs all fail closed.

## Verification

The complete F05 full-multiscale successor regression set passed 263 tests. The focused provider and adapter set passed 72 tests. Dedicated tests prove deterministic provider-owned injection, source-frame immutability, rejection of both pre-injected reserved columns, rejection of outer-test rows, rejection of missing scope, and preservation of the later D+1 mechanics-field blocker. Ruff and `git diff --check` pass on the changed surfaces.

## Next Gate

Formal economics remains locked until these exact public bytes are committed and annotated-tagged. A new formal-v8 execution manifest must bind that clean identity and independently rerun source, panel, fold, owner-policy, predicate-view, and mechanics preflight checks. Formal-v7 manifests and preflight artifacts may not authorize, seed, or resume formal-v8 economics.

No economic outcomes, Validation, or sealed holdout were read while producing this amendment. Research, action, and live authority remain false for the successor.
