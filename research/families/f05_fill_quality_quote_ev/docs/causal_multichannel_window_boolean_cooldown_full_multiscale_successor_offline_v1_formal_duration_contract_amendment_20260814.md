# F05 Offline Full-Multiscale Formal Duration-Contract Amendment

Last materially modified: 2026-08-14

Status: `implemented_local_pre_economic_clean_binding_required`.

## Boundary

This amendment changes only how the offline formal adapter obtains the already-frozen eight-arm duration vocabulary. It does not change any duration, candidate ladder, active owner policy, live runtime, EC2 configuration, lifecycle behavior, action authority, or live authority. Validation and sealed holdout remain unread.

## Formal V8 Stop

Formal-v8 was bound to the clean formal fold-scope tag and passed its mechanics preflight. The first outer fold then queued ten BUY outer-train day jobs. Each job rebuilt its canonical day projection and stopped before control simulation, candidate simulation, duration-fork execution, label generation, or PnL access because the old duration study's full `_load_contract()` attempted to open a historical operational-baseline pointer whose exact private bytes are intentionally unavailable. The adapter did not substitute the current baseline pointer.

The ten progress receipts all have state `failed` and detail `StudyError`; the cache contains zero admitted entries. The formal-v8 output contains only its execution manifest and preflight and has no `formal_result.json`. Formal-v8 is a pre-label implementation diagnostic and cannot be resumed after this source amendment.

## Corrected Duration Boundary

The successor needs the old study only for the frozen outcome-blind duration action definitions and the already-tested duration-arm executor. It does not need the old study's baseline pointer, 40-day plan, denominator, or historical result. The adapter now verifies the exact outcome-blind duration input identity `965400c6...7035`, reads only its candidate-action section, reconstructs all eight BUY and eight SELL actions, and checks every policy ID, second-to-millisecond conversion, engine action, and nonempty duration semantics against the successor's frozen side-specific vocabulary.

The resulting action-universe SHA is `43f77ba5...df96`. Its formal preflight binding states `historical_operational_baseline_read=false`, `historical_execution_plan_read=false`, and `economic_outcomes_read=false`. The full old `_load_contract()` is no longer called by one-shot label execution. The exact current owner B0 remains independently bound by the formal execution manifest and replay adapter.

## Verification

The focused provider and adapter suite passed 73 tests, and the complete full-multiscale successor suite passed 264 tests. A dedicated isolated-module test makes the historical `_load_contract()` fail immediately if called and proves the new loader still reproduces both exact side vocabularies. Preflight tests require the duration binding and its explicit no-baseline/no-plan fields. Ruff and `git diff --check` pass, and the public and private governance audits both report zero findings.

## Next Gate

These exact bytes must now be committed and annotated-tagged. A fresh formal-v9 manifest and preflight must bind that clean identity; no formal-v8 progress receipt, cache key, manifest, or preflight may authorize formal-v9 execution.

No economic outcomes, Validation, or sealed holdout were read while producing this amendment. Research, action, and live authority remain false for the successor.
