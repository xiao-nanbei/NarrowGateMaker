# F05 Offline Formal V23 Execution Design

Last materially modified: 2026-08-16

Status: `formal_v23_implemented_local_pre_admission`.

## Question

Formal-v23 asks only whether the two parity defects observed in formal-v22 can be corrected without changing the frozen research question, and whether a cheap real-data gate can prevent another full-day lockstep from running when the same class of defect remains.

## Immutable Research Identity

Formal-v23 must preserve the exact formal-v22 30-day source manifest, 3,516 mechanics opportunities and ordering, exact current-owner B0 policy and predicate bundle, eight side-specific duration arms, candidate ladder, four-by-three chronological folds, sequential repeated-policy estimand, `action_alpha_v1` statistics, and false action/live permissions.

The v22-to-v23 invariance receipt must compare the source, panel, days, opportunities, folds, B0 artifacts, duration vocabulary, candidate ladder, estimand, statistics, evidence boundaries, and all semantic-source bytes. The only permitted changes are the two parity corrections, the first-opportunity all-arm preflight, versioned execution identities, rebuilt C++ extension bytes, clean commit and tag, and newly generated immutable receipts.

## Parity Corrections

Python and C++ must canonicalize an absent or blank cooldown fallback reason to the same null value before hashing policy decisions. C++ must record the terminal requote and associated summary statistics before applying the same post-requote economic-washout check used by Python.

The common lockstep digest now explicitly covers fallback reason, matched-rule index, requote count, fill path, campaign path, inventory, terminal reason, and existing summary/path identities. A mismatch in any covered field fails closed.

## Admission Gates

Before any real economic qualification path, the unchanged all-panel zero-economic builder walk must accept exactly 3,516 rows with zero evaluator calls. Next, the first admitted real opportunity must run all eight legal side-specific duration arms through Python shared-prefix authority and C++ direct replay with zero digest mismatches. Only after this cheap gate passes may the full first-day 81-opportunity, 648-arm lockstep run.

The quick and full locksteps may compute path state solely for implementation parity, but neither may persist or expose economic values or use them for selection. Every receipt must bind the clean commit, annotated tag, execution manifest, v22-to-v23 invariance receipt, source and panel manifests, exact B0 artifacts, observation tape, C++ extension, and execution-source hashes.

## Engine Boundary

C++ authority remains limited to outer-train one-shot label generation after qualification. Outer-test B0-versus-candidate evaluation remains Python-authoritative sequential repeated-policy replay. One-shot effects may not be summed into policy PnL, and this identity grants no C++ outer-test, live, action, or deployment authority.

## Stop Rule

Failure of the 3,516-row builder walk, first-opportunity eight-arm gate, full qualification day, manifest/hash validation, or formal preflight permanently closes formal-v23. No formal-v24 may be created automatically. Nested OOF may begin only after every pre-economic gate passes.

## Forbidden Operations

Formal-v23 must not change or deploy EC2, create a shadow or companion, modify the active owner policy, read Validation or sealed holdout, change dates or folds, alter the candidate ladder, tune durations or model complexity, change the estimand or statistics, or grant action/live authority.
