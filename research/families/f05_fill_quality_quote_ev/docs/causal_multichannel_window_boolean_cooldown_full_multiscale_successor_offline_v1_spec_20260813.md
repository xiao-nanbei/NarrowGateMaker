# F05 Full-Multiscale Successor Offline V1

Last materially modified: 2026-08-13

Status: `formal_preflight_pending_clean_annotated_tag`.

## Research Identity

| Field | Frozen value |
| --- | --- |
| `identity` | `causal_multichannel_window_boolean_cooldown_full_multiscale_successor_offline_v1` |
| `panel_role` | `family_specific_unconsumed_historical_development` |
| `evidence_clock` | `exchange_time` |
| `queue_identity` | `modeled_queue_with_same_millisecond_ambiguity_censoring` |
| `execution_mode` | `offline_only` |
| `live_runtime_changes_allowed` | `false` |
| `new_research_writer_allowed` | `false` |
| `candidate_specific_capture_allowed` | `false` |
| `action_authorized` | `false` |
| `live_authorized` | `false` |

This identity asks whether a bounded but faithful full-multiscale Boolean cooldown learner has sequential campaign value relative to the exact active owner policy when trained and evaluated on family-specific historical Development dates not consumed by the exact F05 cooldown-selection chain.

The evidence is historical exchange-time Development. It is not prospective, globally fresh, receive-time authoritative, strict exchange-queue authoritative, exact live-lifecycle evidence, current-host transport parity, or independent confirmation. Same-millisecond trade/book events without a common sequence remain censored rather than being assigned an invented order.

No live shadow, companion, observer, writer, journal, feature dump, hypothetical-action log, candidate telemetry, deployment, restart, or EC2 configuration change is permitted. The current owner policy remains the exact primary baseline and is not modified by this study.

## Current Authority

The unique non-injectable formal orchestrator and fixed backend contract now exist. The outcome-blind source gate admitted the frozen first 30 candidates without replacement. Its canonical source manifest, the 36 context-day normalized book view, the 864-hour native cache manifest, the complete labels-disabled causal feature-v2 manifest, and the 34-day native observation batch are owner-side artifacts in the private evidence store and are not distributed with the public repository; their machine-readable identities are recorded in the Spec JSON. The five-file mechanics panel is atomically admitted with 3,516 common opportunities, exact owner B0 actions, and explicit false declarations for labels, candidate actions, and economic outcomes. Formal economics remain prohibited until a clean annotated source tag binds this immutable admission chain and the formal preflight completes.

No economic outcomes, Validation, or sealed holdout have been read for this identity. No research, action, or live authority exists, and this document does not claim that nested out-of-fold evaluation, final refit, repeated-policy replay, or economic testing has completed.

## Frozen Date Universe

The exact F05 cooldown-selection chain already consumed the following target dates, which are excluded from assignments and economic rows for this identity:

```text
2026-06-29
2026-07-03
2026-07-04
2026-07-05
2026-07-06
2026-07-07
2026-07-08
2026-07-09
2026-07-10
2026-07-16
```

The first 30 chronological historical candidates are frozen as:

```text
2026-06-27
2026-06-28
2026-06-30
2026-07-01
2026-07-02
2026-07-11
2026-07-12
2026-07-13
2026-07-14
2026-07-15
2026-07-17
2026-07-18
2026-07-19
2026-07-20
2026-07-21
2026-07-22
2026-07-23
2026-07-24
2026-07-25
2026-07-26
2026-07-27
2026-07-28
2026-07-29
2026-07-30
2026-07-31
2026-08-01
2026-08-02
2026-08-03
2026-08-04
2026-08-05
```

The only permitted replacement order is `2026-08-06` through `2026-08-11`, in chronological order. A failed candidate may be replaced only by the next eligible backup after an outcome-blind source rejection; no date may be selected or rejected after inspecting PnL or another economic outcome.

Consumed target dates may provide D−1 warmup or D+1 washout context, but they may not create target assignments, training rows, test rows, or economic evidence. The labels `family_specific_unconsumed` and `historical Development` do not imply that another research family has never accessed the same calendar date.

## Outcome-Blind Source Gate

Before any economic access, the study must freeze the 36-date candidate universe, the 10-date exclusion table, the replacement order, and every target's D−1/D/D+1 interval. Each source receipt must bind file identity, hourly coverage, byte count, SHA256, schema, timestamp source, normalization identity, and common causal interval.

The source audit must verify raw snapshot/delta files, snapshot seed, `U/u/pu` sequence behavior, gaps, duplicates, out-of-order records, time reversal, official aggregate trades, individual-trade identity and clock, and one unified causal 100ms normalized BBO/L2 identity. Existing normalized roots may be combined only after schema, transaction-timestamp semantics, 100ms boundaries, depth level, code/config identity, and hashes are proven compatible; otherwise the affected interval must be rebuilt from retained raw sources under one identity.

Every admitted target must also pass outcome-blind support checks for side, opener/add role, consecutive fill units, feature readiness, action support, three-valued missingness, and censor reasons. Same-millisecond trade/book events without a common sequence remain censored, and unsupported actions remain unidentified rather than being assigned neutral zero.

The source gate may inspect metadata, clocks, coverage, feature readiness, support, missingness, and censor identity, but it must not read PnL, terminal campaign value, candidate uplift, or any other economic outcome. If fewer than 30 candidates pass after the frozen backups are exhausted, the identity must emit `blocked_missing_canonical_fields` with the exact date, source, field, clock, and authority deficiency and then stop without creating a live data path.

## Formal Offline Orchestrator

Formal economics must have one canonical orchestrator and command-line entry point. That entry point may accept only a canonical manifest path; it may not accept a DataFrame, a manually supplied date list, a hand-built fold object, a custom evaluator, or a one-shot aggregate.

The orchestrator must run from a clean public commit or annotated tag and must independently revalidate every immutable day-admission receipt, receipt SHA, source SHA, selection SHA, panel SHA, fold SHA, date order, panel role, exact owner baseline identity, score profile, and runtime identity before it permits economic access. Its final report and scorecards must carry the complete source/day/selection/panel/fold receipt chain.

The formal entry point must fail closed when any receipt is absent or duplicated, a manifest or selection hash changes, a panel row or date order changes, a caller substitutes the evaluator, one-shot effects are aggregated as policy PnL, the control is not the exact current owner policy, the input role is Validation or sealed holdout, or the public worktree is dirty.

The generic nested-OOF and repeated-policy components remain injectable research libraries and are not themselves the formal economics interface. Their outputs acquire formal status only when invoked by the canonical orchestrator after its complete admission chain succeeds.

## Model And Search Contract

`B0_CURRENT_EXACT` is the exact active owner policy, not a standalone `CONTROL_85N` arm. BUY remains control within B0; SELL retains the current owner durations and three-valued readiness, safety, stale, and fallback semantics; reducing quotes remain unchanged.

The frozen ladder is `B1_CAMPAIGN_AGE_ONLY`, `B2_CAMPAIGN_PLUS_H16_H256`, `B3_CURRENT_SEMANTIC_EQUIVALENT`, `E1_FULL_EMA_BANK`, `E2_DIRECTIONAL_EMA`, `E3_HIGHER_ORDER_BOOLEAN`, `M2_TRUE_INCREMENTAL`, and one action-rate- and duration-distribution-matched control for each candidate.

E1 must make all 10 half-lives and all 45 fast/slow EMA pairs eligible inside each inner-training fold. E2 must validate direction, last-cross direction, recency, persistence, distance, normalized distance, slope, curvature, convergence, and expansion for every pair. E3 must make multi-rule ordered first-match AND/OR/NOT policies genuinely reachable. M2 may be called incremental only when trade/flow/depth information is tested after the best campaign/EMA representation and the compiled policy actually uses a trade/depth predicate.

Each inner fold independently constructs its identified-only feature pool and freezes its search. Unidentified rows remain missing and may not affect feature ranking, policy selection, or action. Replicating unsupported rows must not alter the selected pool, policy, or action. The hierarchy and its simultaneous statistical family must be identical in the Spec, implementation, and final report, including the continuous-minus-Boolean dominance blocker.

Learning-algorithm OOF evidence, a full-Development final-refit artifact, and later exact-artifact confirmation are three separate identities. Evidence for one may not be relabelled as evidence for another.

## Formal Execution Order

1. Complete the outcome-blind source audit and atomically admit the first 30 passing dates under the frozen candidate and backup order.
2. Freeze the admitted-day manifest, exclusion receipts, source hashes, panel hash, and four expanding chronological outer folds generated from the admitted dates.
3. Inside each outer-training period, generate one-shot counterfactual labels only for training; unsupported action targets remain missing.
4. Inside every inner fold, perform purge and washout, inner-train-only feature census and screening, complexity selection, and policy freeze.
5. On each untouched outer-test period, execute paired sequential repeated-policy replay for `B0_CURRENT_EXACT` and the frozen candidate; every eligible exposure-increasing fill invokes the arm's policy, and one-shot effects are never summed as policy PnL.
6. Report BUY and SELL separately, including the frozen hierarchy, day/week simultaneous bands, matched controls, `action_alpha_v1` scorecard, action and duration mix, role and consecutive-unit support, censoring, three-valued missingness, SMD/PSI, top-day sensitivity, inventory risk, fill retention, and campaign-terminal value.
7. Grant at most learning-algorithm OOF Development evidence from these 30 dates. Only after OOF may a final candidate be refit and frozen; the same 30 dates do not independently confirm that final artifact.
8. After a final artifact is frozen, reproduce immutable historical prefix-40, added-10, and pooled-50 panels only as mandatory baseline and mechanics sensitivity. Those consumed panels cannot select the candidate or grant fresh support.
9. Exact final-artifact confirmation may later use naturally produced canonical data or an owner-authorized real randomized canary. It may not use a shadow or companion.

If the admitted dates are exactly the frozen first 30, the provisional outer tests are dates 11-15, 16-20, 21-25, and 26-30, with expanding training prefixes of 10, 15, 20, and 25 dates. The actual fold manifest must always be generated from the final admitted-day manifest, so outcome-blind replacements preserve chronological order without hand-editing folds.

## Permissions And Stop Rules

Source admission grants data authority only. A passing mechanics audit grants implementation evidence only. Nested OOF grants at most Development evidence for the learning algorithm. Final refit grants an immutable candidate artifact only. None of these stages grants action or live authority.

The study must stop when canonical fields remain insufficient, any formal binding fails, the exact owner baseline cannot be reproduced, sequential repeated-policy evaluation is unavailable, or a statistical or risk hard gate fails. A stop does not authorize a new writer or a change to the current owner policy.

Validation and sealed holdout remain unread. No candidate may be deployed, and no EC2 runtime may be restarted or reconfigured under this identity. Any later action or live decision requires a separate explicit owner authorization and a distinct deployment contract after exact-artifact confirmation and transport evidence.

## Relationship To The Withdrawn Identity

The prospective companion was withdrawn before deployment, with zero rows and no economic read, as recorded in [the withdrawal receipt](causal_multichannel_window_boolean_cooldown_full_multiscale_successor_prospective_v1_withdrawal_20260813.md). This offline identity is a new evidence contract rather than a rename of the withdrawn prospective identity.

The reusable mechanics, nested-OOF, final-refit, repeated-policy, and transport-adapter modules may be retained as pure offline components only when they obey this identity's admission and permission boundaries. The canonical B0 mechanics adapter, fixed historical replay adapter, repeated-policy backend, and manifest-only formal loader are implemented. The 30 target days require 34 observation-context days, with `2026-06-29`, `2026-07-03`, `2026-07-16`, and `2026-08-06` restricted to continuation-only use and forbidden from creating target assignments. The next gate is a clean annotated source tag followed by the immutable formal preflight; no OOF or economic runner may execute before that preflight reports ready. This Spec records completed source and mechanics work rather than declaring the economic study complete.
