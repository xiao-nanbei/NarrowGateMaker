# F04 External Market Alpha

Last materially modified: 2026-08-20

Documentation boundary: this README and the unit's tracked `docs/` are public. Owner-only artifact locators, unpublished evidence indexes, and private research context are resolved through this unit's ignored local `private/` catalog and are not distributed with the public repository. See the [public/private research layout](../../PRIVATE_EVIDENCE.md).

> Publication note: `${NARROWGATE_*}` values and deployment-epoch names are logical locators. Owner-side data and machine artifacts are in the private evidence store and are not distributed with this repository unless a repository-relative link is provided. See the [public/private documentation contract](../../../docs/public_private_documentation_contract.md).

Status: external information-decay and M0/M1 evidence are Development-only. P2 v1 mechanics are complete but action support is insufficient. Its frozen exact-opener v2/v2.1 identities are unchanged. The execution-only v2.2 successor now closes runtime identity, writer health, hot-start quarantine, cancel-reject lifecycle, crash recovery, and atomic `${NARROWGATE_PRIVATE_EVIDENCE_ROOT}` admission contracts. Its original collection preflight passed against a temporary, tape-enabled copy of the private operational config. The later BUY fill-selection shadow/action runtime split changed hash-bound `live/config.py`, `live/main.py`, and `strategy/maker_engine.py` bytes. Frozen v2.2 is therefore historical and fail-closed; a successor identity is required before prospective collection. The actual public/private tapes remain disabled. External feeds have no quote-action or live authority.

This directory owns historical external features, consensus construction, reference replay, model training, and the shadow panel. Live WebSocket adapters remain under `live/venues/`. Shared dependencies: D, R, G.

The preregistered receive-time successor is [`first_add_external_incremental_value_m0_m1_v1`](docs/first_add_external_incremental_value_m0_m1_v1_preregistration_20260730.md). It compares campaign-state plus local causal microstructure against the same model with Bitget/Bybit/OKX AWS receive-time state, using F10's direct first-add decision-to-campaign-terminal USDC target. Its 30-distinct-day count gate is complete, but it remains blocked until every chronological, true leave-one-venue-out, quality-panel, lifecycle, common-row, causal-clock, and late-panel denominator passes. The older future-direction and maker-lifecycle screens cannot substitute for this identity.

The source-aware seven-tape program is complete: immutable v1 plus source-bound v2 contain 31 valid full windows over 30 distinct UTC days, split across 21 original-AWS days, one intermediate-Vultr day, and eight reactivated-AWS days. The collection automation is deleted and the current host has no inherited capture authority. Reaching 30 days closes only the count gate; P1 remains blocked until the corrected unknown-submit-ACK lifecycle code is deployed, a successor exact-lifecycle session is admitted, and every frozen chronological, side-specific, Grade-A/Grade-B, common-row, source-transport, true-LOO, causal-clock, and late-panel gate passes. The frozen [`transport-source amendment v2`](docs/first_add_external_incremental_value_m0_m1_v1_transport_source_amendment_v2_20260811.json) retains its then-current reactivated-AWS source as a historical stratum and must not be dynamically rebound to the current host. A passing side would still provide prediction evidence only; an action requires a separate preregistration.

At `2026-08-11T15:48:02Z`, the live host returned to AWS Tokyo. The outcome-blind [v2 transport-source amendment](docs/first_add_external_incremental_value_m0_m1_v1_transport_source_amendment_v2_20260811.md) froze `aws:ap_northeast_1:<current-live-epoch>` as the third capture stratum then current; that token now names the reactivated-AWS predecessor and must remain historical. Original-AWS v1 and retired-Vultr rows remain immutable, and the current successor host is not an admitted fourth capture stratum.

Capture collection is closed and its automation is deleted. M0/M1 remains blocked on an exact-lifecycle successor: both the retired Vultr session and the reactivated-AWS predecessor session reproduced the never-activated GTX `-5022` exchange-exposure encoding mismatch. Those journal sessions are invalid evidence even though maker trading continued safely; the current host's admitted operational lifecycle session does not silently repair that research identity.

The project-level [BABEL map](docs/babel_external_market_research_map.md) keeps the routes parallel rather than serial. P1 has completed its count gate but is stopped at the exact-lifecycle and row-level gates; independently, the Development-only [`external_adverse_quote_edge_guard_mechanics_v1`](docs/external_adverse_quote_edge_guard_mechanics_v1_spec_20260802.json) can inspect receive-time visibility, all/LOO direction, role support, outward quote movement and cap clipping without reading reward. Its denominator contains all paired quote decisions, not exact first-add decisions, so it cannot inherit P1 prediction evidence or register an F09 action.

The [Development mechanics report](docs/external_adverse_quote_edge_guard_mechanics_v1_development_20260802.md) retains this as clock-limited evidence. Seventeen windows over 16 days supplied 9,138 paired quote opportunities. There were 26 outward coordinate changes among 7,786 eligible opener/add side-opportunities; only six occurred on add quotes. The report also corrects a live-log semantic trap: `inventory_ratio` is an unsigned magnitude, so role is derived from a separately hash-bound, strictly-prior signed-position state journal. Exact decision time, two quote-log days, and authoritative order/ACK/queue paths remain unavailable.

That observed candidate rate is only 0.334%, versus the frozen 5% minimum in `action_execution_selective_v2`. A 0.5 randomized assignment would therefore change only about 13 paths. P2 v1 is retained as successful mechanics evidence, but it is not eligible for randomized action registration and no PnL was read.

The registered infrastructure successor, [`external_adverse_quote_edge_guard_exact_opener_mechanics_v2`](docs/external_adverse_quote_edge_guard_exact_opener_mechanics_v2_spec_20260802.json), adds stable decision IDs, exact decision/feature-ready clocks, signed inventory, baseline and shadow coordinates, and native submit/activation/cancel/fill lineage. Its formal denominator is exact opener opportunities, not P1 first-add. The collection-time [`v2.1 execution amendment`](docs/external_adverse_quote_edge_guard_exact_opener_mechanics_v2_1_20260802.md) now enforces an exact schema allowlist, stable non-null identifiers, unique strict lifecycle sequences, at most one terminal outcome per order, and exact baseline/candidate denominator rules. Operational fill and terminal events are read only for lifecycle mechanics; economic outcomes remain unread. The 5% support floor must pass separately for BUY and SELL, while the pooled rate is diagnostic only.

A subsequent [`collection preflight`](docs/external_adverse_quote_edge_guard_exact_opener_mechanics_v2_1_collection_preflight_failure_20260802.md) found that v2.1 does not hash-bind the maker/order runtime producers, fail closed on source/config dependencies, expose CSV writer failure to formal admission, quarantine inherited orders, journal REST cancel rejection, or atomically admit bounded daily parts to `${NARROWGATE_PRIVATE_EVIDENCE_ROOT}`. The [`v2.2 execution contract`](docs/external_adverse_quote_edge_guard_exact_opener_mechanics_v2_2_20260803.md) closes those implementation gaps without modifying the frozen predecessors. The enabled-config preflight originally passed the three-venue, stablecoin-anchor, shadow-only, local-staging, mounted-`${NARROWGATE_PRIVATE_EVIDENCE_ROOT}` and runtime-identity gates. The 2026-08-03 runtime permission split subsequently invalidated its implementation hashes without changing the frozen contract. No prospective row has been read and no deployment occurred. A new execution successor must bind the current runtime before any bounded writer/admission soak. BUY/SELL support counting begins only after valid atomic admission. If either side then remains below 5%, this guard closes for action support. Removing LOO or introducing a rank budget requires a separate future identity.

The separate Development-only [`cross_venue_causal_fair_price_v1`](docs/cross_venue_causal_fair_price_v1_spec_20260801.json) constructs a two-of-three Bitget/Bybit/OKX fair price from causal historical one-second individual-trade bars. It removes past-only venue basis, converts through USDCUSDT, uses a robust weighted median, and shrinks the local-to- external lead by an outcome-blind online gain. The all-venue and three true leave-one-venue-out variants share one common validity mask. Overall common support is 89.65%, but these provider-clock historical bars do not establish AWS receive-time live transport.

Its independent F09 action sibling, [`cross_venue_fair_center_shift_randomized_replay_v1.1`](../f09_campaign_action_uplift/docs/cross_venue_fair_center_shift_randomized_replay_v1_1_development_20260801.md), shifted the complete bid/ask pair while preserving spread and the rest of the strategy path. The 24-day Grade-A reward point estimate was positive, but its lower bound crossed zero and the leave-Bybit-out reward and USDC/day directions were negative. That action is closed on Development. This result does not modify or unlock the separately preregistered first-add receive-time M0/M1 identity.

The later EC2 live prefix has now been used for its intended engineering purpose. Across 60,977 common quote-snapshot rows, replay reproduced shift ticks, candidate prices, GTX clamps, and pair-spread preservation exactly. That closes coordinate transport, not economic transport: the frozen Grade-A value interval still crosses zero and leave-Bybit-out remains negative. The continuous fair-center shadow log was therefore retired on 2026-08-08; the independent bounded F04 recorder was unaffected by that retirement, then later completed and was deleted after closing on the reactivated-AWS predecessor. See the [`live shadow retirement audit`](../f10_live_replay_attribution/docs/live_shadow_retirement_audit_v1_20260808.md).
