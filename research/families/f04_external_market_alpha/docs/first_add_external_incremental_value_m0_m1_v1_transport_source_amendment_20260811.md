# First-Add External M0/M1 Transport-Source Amendment

Last materially modified: 2026-08-11

Status: `preregistered_blocked_on_30_distinct_valid_source_aware_receive_time_utc_days`

The parent v1 audit remains the only permitted outcome model. This amendment does not change its target, Ridge estimator, M0/M1 economic question, side split, Grade A/B rules, leave-one-venue-out rebuilds, common denominators, statistical gates, or prediction-only permissions.

It changes only the transport admission identity after the live host migrated from AWS Tokyo to Vultr Tokyo before the AWS-only ledger reached 30 distinct full-window UTC days.

The 21 admitted AWS full-window UTC days remain an immutable historical stratum. Prospective Vultr captures form a separate host-bound stratum. The two strata may contribute to the 30-day chronological panel only when every row preserves provider, region, host/runtime/config epoch and causal receive / feature-ready clocks. A UTC day counts once across all sources. Captures below 3,500 seconds remain diagnostics.

AWS rows must not be relabelled as Vultr, and Vultr rows must not be stored in `aws_tokyo_*` directories or appended to an unlabelled AWS ledger. Frozen AWS latency profiles may be reused for AWS reproduction or an explicitly named historical sensitivity. They are not current Vultr transport measurements.

The host/source identity is a provenance and stratification variable, not a new alpha feature. Standardization must be fitted from past rows within the available source strata. Report the combined chronological result and each supported source stratum; a material source-direction reversal blocks support.

No target has been read for this amendment. The audit remains blocked until 30 distinct admitted full-window UTC days and every parent chronology, side-specific, Grade-A-primary/Grade-B-sensitivity, exact-lifecycle, true-LOO, common-row and late-panel denominator passes.
