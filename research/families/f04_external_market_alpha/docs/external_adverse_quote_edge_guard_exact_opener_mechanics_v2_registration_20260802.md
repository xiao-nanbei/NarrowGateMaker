# External Adverse Quote Edge Guard Exact-Opener Mechanics v2

Last materially modified: 2026-08-02

Status: infrastructure registered; prospective exact tape not yet collected; no prediction, action, Validation, holdout, or live authority.

## Frozen Interpretation

P2 v1 remains a successful clock-limited mechanics diagnostic and an insufficient action-support result. Its 26 changes among 7,786 eligible opener/add side opportunities imply a 0.334% candidate rate, about fifteen times below the frozen 5% `action_execution_selective_v2` floor. With a 0.5 assignment probability, only about 13 paths would have changed.

The 500ms BUY and SELL survival rates are descriptive counts over 18 and 8 triggers. They are not side-specific transport evidence. Twenty of the 26 changes were opener opportunities and only six were add opportunities, so this surface cannot be merged with P1's exact first-add outcome identity.

## What v2 Adds

The new native journal records one stable decision identity and the exact decision-visible signed inventory, feature-ready time, baseline and shadow candidate coordinates. Orders submitted by that decision retain the origin identity through REST acknowledgement, activation, cancel request/ACK, partial/full fill, rejection, expiry, and queue reset.

The candidate remains shadow-only. It never changes an order. The journal is disabled by default and is not silently added to the existing seven-tape bounded capture contract.

The v2 validator is outcome-blind. It rejects PnL, markout, reward, future-price or campaign-terminal columns and fails closed on incomplete BUY/SELL decision pairs, unsigned role inference, future feature clocks, missing submit lineage, or lifecycle sequence regression.

## Decision Boundary

The formal v2 denominator is exact baseline-eligible opener decisions only. BUY and SELL are reported separately. If the unchanged conservative all/LOO guard remains below the frozen 5% candidate rate, this guard is closed for action support. Removing LOO or introducing an outcome-blind rank budget would be a different future identity; neither can rewrite v1 or v2.

P1 continues independently toward its 30-day receive-time requirement. v2 does not enter F09 and does not read P2 economics.

## Verification

The frozen producer, runtime projection, lifecycle callback, validator and scorecard hashes are recorded in the v2 Spec together with the `cross_venue_causal_fair_price.v1` Feature DAG hash. Runtime projection parity against the frozen v1 implementation is covered for positive, negative and LOO- disagreement states. The complete repository suite passed with 1,322 tests and 4 skips. No prospective tape, PnL, Validation or holdout was read.
