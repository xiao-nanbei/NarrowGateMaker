# F09 Campaign Action Uplift

Last materially modified: 2026-08-10

Documentation boundary: this README and the unit's tracked `docs/` are public. Owner-only artifact locators, unpublished evidence indexes, and private research context are resolved through this unit's ignored local `private/` catalog and are not distributed with the public repository. See the [public/private research layout](../../PRIVATE_EVIDENCE.md).

Status: mechanism research active; no action family is authorized. The latest current-stack fixed-value governance identity, [`ber_guard_role_safe_add_only_current_stack_owner_v1`](docs/ber_guard_role_safe_add_only_current_stack_owner_v1_development_20260809.md), is closed on the frozen 40-day Development panel. The candidate changed 14.96% of side quote decisions and passed every Python/C++ and role-source mechanics gate, but worsened terminal MTM by `-11.666288 USDC` and closed-campaign value by `-9.384488 USDC`; only 13/40 days improved and fills increased 13.85%. Although q10/CVaR, MAE, inventory time, and maximum inventory improved, the primary value and negative-terminal gates failed. Keep current global BER; do not advance the add-only role contract or its proposed directional-BER v2 successor, and do not run continuous confirmation.

The current pooled daily-fresh-start denominator is [`current_live_held_ber_replay_baseline_50d_20260810`](../f10_live_replay_attribution/docs/current_live_held_ber_replay_baseline_50d_20260810.json). Its immutable 40-day prefix records terminal MTM `-144.251748 USDC`, closed-campaign value `-147.466348 USDC`, and 17,118 fills; the pooled 50 days record `-165.566079 USDC`, `-168.530979 USDC`, and 20,147 fills. Replay samples the last completed canonical 10-second `trade_intensity_60s` value on each completed 1-second callback, matching the live BER feature clock.

The current 40/50-day implementation did not consume raw snapshot/delta queue events, empirical REST latency, or AWS receive/feature-ready visibility delay. It is therefore a common-simulator diagnostic denominator, not an action promotion baseline. Every price, replace, cooldown, cancel/re-entry, or fill mechanism must bind the strict-native latency successor when that successor is admitted. Historical 40-day candidate reports remain unchanged as records of their original denominator.

The current-identity plumbing successor [`causal_v12_ranked_toxicity_exposure_guard_carryover_safe_v2_2`](docs/causal_v12_ranked_toxicity_exposure_guard_carryover_safe_v2_2_execution_result_20260803.md) regenerates untreated baseline shadow and candidate under the same loader, code, and read-only cache identity. Its 2026-04-17 smoke consumed all 29,072 baseline rows and recorded zero baseline-shadow mismatch, cross-arm ownership, forced-washout cancel, or owner mismatch on both sides. The 58 BUY and 50 SELL carryover transitions passed their lifecycle contract. This is a one-day, outcome-blind plumbing result only: the formal 40-day mechanics run, PnL, Validation, holdout, action authority, and live authority remain unopened. It was bound to the historical v5 operating snapshot with BUY fill-selection enabled in both arms. The active v7 baseline now has that action suspended and keeps only shadow scoring, so this smoke is no longer current-baseline-equivalent. A successor linkage and execution identity are required before any formal mechanics run.

The mechanics-only [`registration_linkage_v3`](docs/causal_v12_ranked_toxicity_exposure_guard_registration_linkage_v3_amendment_20260803.json) binds the historical v5 config hash `832e...` to the same frozen causal-v12 bundle used by both side registrations. After the v7 operational permission split, the current config hash is `ef76fe...`; v3 is therefore preserved as historical mechanics evidence and correctly fails against current config bytes. Neither v2 nor v3 may be relabeled as current, and no threshold, seed, scorecard, outcome, or permission changed.

The Development-only `volatility_time_add_rearm_feasibility_v1` identity is closed on mechanics. Its individual-trade-close variance clock failed causal availability and remains immutable. The successor `volatility_time_add_rearm_feasibility_v2_1` uses completed-bucket normalized BBO and passes all frozen clock-mechanics gates for BUY/SELL across 0ms, 250ms, and 1000ms ready-delay scenarios, with zero Python/C++ mismatches. Its [Development report](docs/volatility_time_add_rearm_feasibility_v2_1_development_20260729.md) is evidence-only.

The [live blocker-attribution diagnostic](docs/volatility_time_add_rearm_blocker_attribution_20260729.md) additionally shows that other guards do not make the current fill cooldown mechanically redundant. The infrastructure-only [`replay_path_dependent_blockers_v1`](docs/replay_path_dependent_blockers_v1_spec_20260729.json) defines the required BUY q90, path-dependent consecutive-loss cooldown, and sync-degrade semantics. The successor [`volatility_time_add_rearm_live_stack_parity_v1`](docs/volatility_time_add_rearm_live_stack_parity_v1_development_20260729.md) binds those controls to code, artifacts, D-1 BBO, an environment-matched sync tape, latency identities, and tests. Loss cooldown and sync mechanics have Python/C++ parity, while BUY q90 remains Python-only and fail-closed in C++. The successor [`volatility_time_add_rearm_full_path_preflight_v1`](docs/volatility_time_add_rearm_full_path_preflight_v1_development_20260729.md) now integrates variance time into the authoritative Python strategy path. Over 40 Development days, the candidate regenerates orders, fills, inventory, queue, cooldown lineage, BUY q90, loss cooldown, sync stress, and downstream blockers. Unmasked action-effective support is 72.75% for BUY and 57.04% for SELL, with day-cluster 95% lower bounds of 65.28% and 49.59%. The mechanics support gate passes and opens only native C++ BUY q90 parity work.

The Development-only [`volatility_time_add_rearm_cpp_q90_parity_v1`](docs/volatility_time_add_rearm_cpp_q90_parity_v1_development_20260729.md) now passes strict lockstep native-book and BUY-q90 kernel parity for both the wall-time control and variance-time candidate. Across 15,091,226 book-message consumptions and 7,154,847 q90 evaluations it recorded zero mismatch, with real cancel, ACK, recovery, queue-reset, and re-entry coverage. Python remains the authoritative full-path replay; this result does not claim full C++ tick-replay authority.

The Development-only [`volatility_time_add_rearm_randomized_replay_v1`](docs/volatility_time_add_rearm_randomized_replay_v1_development_20260729.md) then randomized the complete regenerated path at the same-side cooldown- lineage level with exact 0.5/0.5 propensity. The intervention had robust final-action support and retained 104.80% of BUY fills and 99.41% of SELL fills, but neither side obtained a positive UTC-day-clustered primary-reward lower bound. Tail, lifecycle, and inventory consistency gates also failed. This variance-time action identity is closed on Development; Validation and sealed holdout remain unread, and no action or live permission was created. AWS receive-time remains a separate transport/live gate.

The post-result [`volatility_time_add_rearm_negative_result_attribution_v1`](docs/volatility_time_add_rearm_negative_result_attribution_v1_development_20260729.md) diagnostic explains the apparent outcome disagreement without reopening the family. BUY's positive campaign-terminal lower bound materially included PnL earned before lineage assignment; its post-decision terminal interval crosses zero. SELL's positive lineage-reward point estimate was offset by negative post-lineage continuation. Diagnostic volatility slices are hypothesis-only, and exact earlier/later or consecutive-fill-unit attribution remains unsupported on the frozen randomized panel.

The shared [`lineage_randomized_outcome_contract_v2`](docs/lineage_randomized_outcome_contract_v2.md) now fixes the foundation for any genuinely new F09 action. Its primary outcome starts at assignment, pre-assignment campaign PnL is limited to a covariate or balance diagnostic, and the lineage reward plus post-lineage continuation must equal decision-to-campaign-terminal value exactly. Future identities must preregister day-by-side randomization and covariate adjustment, and must emit a complete native lineage trace rather than matching against an older mechanics path. This foundation has no Development-result, Validation, action, or live authority.

F10 subsequently confirmed that first exposure-increasing add decisions have negative average decision-to-terminal value on both sides, but F05's chronological OOF model did not identify a stable decision-visible negative value subset on either side. F09 therefore has no registered successor action. A future action requires genuinely new evidence or features, a distinct policy lever, and a new frozen identity; post-result volatility bins or the failed F05 risk scores cannot define it.

The Development-only [`cooldown_release_one_cycle_mechanics_reaudit_v1`](docs/cooldown_release_one_cycle_mechanics_reaudit_v1_development_20260730.md) replayed the current stack over the frozen 40 days. Roughly 99% of release episodes could change the next add-cycle action, but only 7.16% of selected BUY cycles and 6.26% of selected SELL cycles filled; both 95% upper bounds remain below the frozen 10% near-noop boundary. The exact one-cycle intervention is therefore reconfirmed as low leverage and remains closed. Its C0/C1 semantics overlap the already closed first-add skip, SELL one-cycle skip, and recovery- event rearm studies, so the result cannot register a randomized action.

The retrospective Development-only [`cooldown_action_leverage_frontier_v1`](docs/cooldown_action_leverage_frontier_v1_development_20260730.md) then places the completed temporal-permission interventions on one explicitly heterogeneous evidence frontier. The current one-cycle and variance-time metrics retain authority; older state-conditioned, recovery-event, and stop-until-flat numbers remain withdrawn scale diagnostics whose closure decisions are still authoritative. No cross-source estimate is pooled. The tested range runs from weak one-cycle fill-path leverage, through unsupported multi-cycle and variance-time actions, to stop-until-flat participation shutdown. No row combines acceptable activity, a positive reward lower bound, and supported lifecycle evidence. The tested cooldown temporal-permission action subspace is therefore exhausted, while F09 itself remains open for a genuinely different economic intervention.

Here `temporal-permission action subspace` means only the frozen one-cycle, multi-cycle, variance-time, recovery-event, and stop-until-flat identities in that frontier. It does not include every state-conditioned duration policy. F05's later multichannel Boolean EMA duration successor was evaluated separately. Non-control BUY and SELL rules entered nested outer OOF, but none of the 14 side x feature-family gates supported a repeated-policy successor. Its exact no-policy closure therefore creates no F09 action and does not identify 85 seconds as optimal.

The accompanying [`cooldown_action_leverage_frontier_v1` provenance errata](docs/cooldown_action_leverage_frontier_v1_errata_20260730.md) clarifies that the report's comparison with the observed first-add loss scale was not bound to the F10 artifact and is contextual only. The F10 report was not read by the evaluator and did not enter a decision gate. The frozen Spec, results, MDE/fill-rate calculations, and temporal-permission closure remain unchanged.

The distinct cumulative-inventory candidate has now completed its formal Development mechanics audit. The [`post_cooldown_incremental_inventory_budget_feasibility_v1_2` report](docs/post_cooldown_incremental_inventory_budget_feasibility_v1_2_development_20260801.md) uses 24 explicit Grade-A primary days and 16 Grade-B sensitivity days, with BUY q90 disabled identically in both arms. It limits later exposure-increasing fill units after normal cooldown release while leaving reducing quotes open.

The Grade-A unlimited control produced only one nonzero whole-unit candidate on both sides: `{BUY: [1], SELL: [1]}`. SELL lineages exceeded one actually filled unit only 3.31% of the time. The B=1 path nevertheless changed 86.14% of submitted-order episodes while retaining 98.89% of fills. It therefore changes many replace/submission paths but has little leverage over marginal fills, and it exceeds the frozen maximum action-change rate. The exact post-cooldown cumulative-budget surface is closed for insufficient grid resolution and unsupported mechanics; no randomized economic identity was created and no PnL, Validation, or holdout was read.

This result does not close every inventory intervention. It shows that the post-cooldown lineage is the wrong control surface for obtaining a useful 1/2/3-unit frontier under the current lifecycle. Future work may not rescue it by changing quantiles after the result or by pooling Grade B into the primary gate.

F10's subsequent [`live_240h_loss_solution_routing_v1`](../f10_live_replay_attribution/docs/live_240h_loss_solution_routing_v1_20260801.md) strengthens the mechanics motivation for reporting SELL/SHORT separately. Single-level SHORT campaigns were approximately flat, while 257 multi-level SHORT campaigns contributed -11.1471 USDC. This is observational mechanism localization, not action evidence. The candidate can only change later post-cooldown exposure-increasing fills; it cannot repair SELL opener quality.

The separate q90 terminal-hold audit remains an execution boundary. v1.2 correctly froze q90 OFF in both arms, so this closure is not contaminated by the unresolved q90 hold lifecycle. It is also not current live-baseline equivalence; q90 repair and economic attribution remain separate work.

The subsequent Development-only [`sell_add_inventory_price_penalty_randomized_replay_v1`](docs/sell_add_inventory_price_penalty_randomized_replay_v1_development_20260801.md) tested a distinct continuous price intervention rather than another quantity budget. While already SHORT, candidate SELL-add quotes moved outward by the single frozen curve `min(1.5, 0.5 * short_inventory_units)` bps; flat SELL openers and reducing BUY quotes were unchanged. Campaigns were randomized once at exact 0.5/0.5 propensity before the downstream path was generated, and BUY q90 was OFF in both arms.

The action was mechanically real and uncapped: Grade-A final-action change was 100%, cap truncation was zero, and the realized/requested penalty ratio was 101.46%. It reduced maximum inventory, inventory time, repair time, and MAE, but retained only 60.98% of SELL-add fills and 73.74% of aggregate activity. The primary assignment-to-terminal reward was -0.001339 USDC with day-clustered 95% interval `[-0.011489, +0.009242]`; multi-level SHORT loss protection was also slightly negative with an interval crossing zero. Grade B reproduced the same direction. The fixed price curve is therefore closed on Development, with `ranking_score=null`; Validation, sealed holdout, action, and live permissions remain false. A future reducing-BUY intervention must use a new, separate frozen identity and may not retune this consumed price curve.

The subsequent Development-only [`multi_short_reducing_buy_aggression_v1.1`](docs/multi_short_reducing_buy_aggression_v1_1_development_20260801.md) tested that separate reducing-BUY intervention. At the first transition to at least two SHORT units, the candidate kept a GTX reducing BUY at the most aggressive valid maker price until inventory recovered to one SHORT unit. The 24-day Grade-A action-change rate was 99.37%, total fills were retained, and the treatment reduced inventory time and repair time with positive lower bounds. It nevertheless produced -0.004435 USDC/assignment primary uplift, only 45.83% positive UTC days, no positive terminal-value lower bound, and slightly worse q10/CVaR10. Grade B was also negative. This exact aggressive maker repair action is closed on Development with `ranking_score=null`; Validation, holdout, action, and live permissions remain false. No bounded IOC successor was created.

Taken together with the closed SELL-add price-penalty action, this exhausts the tested **inventory suppression and passive repair** action subspace. One action reduced entry into multi-level SHORT inventory by deleting participation; the other retained activity and accelerated passive repair, but neither improved assignment-to-terminal value or tail risk. Inventory time, repair time, MAE, and maximum inventory are therefore mechanism diagnostics rather than substitutes for terminal value. The current reducing-BUY baseline is retained, no IOC successor will be tested on this consumed Development panel, and trigger inventory, maker price, or release conditions may not be retuned. F09 itself remains open only for a genuinely different economic intervention supported by new decision-visible evidence.

The canonical current route is therefore `inventory_suppression_and_passive_repair_action_subspace_exhausted`. The q90 terminal-risk-set repair is separate baseline/replay integrity work, not a PnL successor and not an F09 prerequisite when every arm freezes q90 OFF. No closed inventory, quote-gradient, temporal-permission, or aggressive-repair action may be reopened merely because q90 mechanics or the markout-side sign are repaired.

F04's preregistered `first_add_external_incremental_value_m0_m1_v1` continues as an independent background evidence stream. It cannot run before 30 different valid receive-time UTC days and cannot itself authorize an action, but that denominator does not block F04's outcome-blind E6/P2 mechanics. The new `external_adverse_quote_edge_guard_mechanics_v1` is not an F09 action: it may measure trigger support, LOO consistency and outward quote movement only. Its all-quote opportunity surface also cannot borrow P1's first-add prediction. No randomized guard identity exists until prediction and mechanics refer to the same exact denominator.

That mechanics identity is now complete on its historical clock-limited denominator: 26 coordinate changes occurred among 7,786 eligible opener/add side-opportunities, with only six add triggers. This preserves a possible mechanism while confirming that neither action support nor the exact first-add surface has been established. F09 remains without a registered guard action.

The subsequent Development-only [`cross_venue_fair_center_shift_randomized_replay_v1.1`](docs/cross_venue_fair_center_shift_randomized_replay_v1_1_development_20260801.md) tested a genuinely different economic lever: a causal, continuous shift of the whole bid/ask pair toward a two-of-three Bitget/Bybit/OKX fair center. Pair spread, order size, inventory and reducing logic, cooldown, queue mechanics, latency, and GTX behavior were held fixed; BUY q90 was OFF in both arms.

On 24 Grade-A days, 9,164 campaigns had exact half propensity, 100% realized action-path change, 97.00% fill retention, and 92.35% activity retention. The reward point estimate was +0.001446 USDC/assignment and HT policy value was +0.6351 USDC/day, but their intervals were `[-0.002732, +0.006053]` and `[-1.0143, +2.3994]`. The positive-day rate was 54.17%, SELL reward was slightly negative, and leave-Bybit-out reward and USDC/day both reversed sign. Grade B was positive but remains sensitivity-only. The action is therefore closed with `ranking_score=null`; Validation, sealed holdout, action, and live permissions remain false. Venue weights, basis windows, gain, side scaling, and eligibility may not be retuned on this consumed Development panel.

This closure does not invalidate F04 external-market research. It says that the exact historical-bar, symmetric fair-center action lacks Grade-A and LOO economic support. The later live shadow completed exact coordinate parity on 60,977 matched decisions, then was retired because further continuous logging could not resolve the frozen counterfactual PnL uncertainty. AWS receive-time prediction may continue under its separate bounded recorder identity, but cannot inherit action authority from this result. See the [`live shadow retirement audit`](../f10_live_replay_attribution/docs/live_shadow_retirement_audit_v1_20260808.md).

The next distinct mechanism is registered as two side-specific mechanics identities, but its formal run is blocked by the carryover failure below: [`causal_v12_ranked_toxicity_exposure_guard_buy_v1`](docs/causal_v12_ranked_toxicity_exposure_guard_buy_v1_mechanics_spec_20260802.json) and [`causal_v12_ranked_toxicity_exposure_guard_sell_v1`](docs/causal_v12_ranked_toxicity_exposure_guard_sell_v1_mechanics_spec_20260802.json). They use only the rank of causal v12 `tox_bid_10s` or `tox_ask_10s`, with a past-only side-specific p90 computed from unique baseline-eligible exposure-increasing 10-second opportunities. The current operational baseline is preserved exactly: q90 remains shadow-only and BUY fill-selection remains enabled in both randomized arms. Both specs hash-bind the production q90 suspension release. The terminal risk-set repair and fresh prospective placement recovery remain local-only, while 40-day lockstep, transport, and live parity remain incomplete.

The candidate is a persistent permission state machine, not a raw toxicity threshold. It cancels an active exposure order once at a rank crossing, suppresses later exposure submissions through cancel ACK, ends the old order's fill-risk set, and releases only on a later completed 10-second rank below the same daily threshold. Reducing quotes never change. BUY and SELL are separate identities with one 0.5/0.5 assignment per campaign-side lineage.

The execution-only [`full-path adapter v1.4`](docs/causal_v12_ranked_toxicity_exposure_guard_full_path_adapter_v1_4_execution_amendment_20260802.json) remains a frozen dependency identity. v1.1-v1.3 remain frozen historical evidence. v1.4 separates unique 10-second prediction updates from every quote decision, uses a stable untreated prospective-lineage identity for the assignment PRF, and binds a two-pass Python-authoritative tick replay that regenerates the candidate order, queue, fill, inventory, and campaign path. Candidate campaign terminals cannot rerandomize the arm; only a change in the untreated baseline lineage can end an assignment. Unknown terminal routes fail immediately. Untreated eligibility controls assignment and denominator, while the regenerated candidate role controls permission, so path divergence cannot accidentally suppress a reducing quote.

Formal journals are streamed as atomic hash-verified Parquet parts on the local cache disk. A one-day authoritative execution preflight subsequently found a more fundamental assignment-contract failure: an exchange-active BUY order submitted as an `add` under one prospective campaign lineage remained resting after a reducing SELL fill flattened inventory, then became an `opener` under the next lineage. Rerandomizing at that boundary would let a new arm control an order created by the prior arm.

The [`v1.5 implementation-failure record`](docs/causal_v12_ranked_toxicity_exposure_guard_full_path_mechanics_v1_5_implementation_failure_20260802.md) therefore withdraws mechanics execution eligibility before the 40-day run. The complete baseline journal is retained as outcome-blind evidence; partial candidate journals are not formal results. No economic column, Validation, or holdout was read. A successor must preregister a carryover-safe assignment unit or washout rule before execution; this cannot be patched into the frozen campaign-side identity. C++ full-path authority is not claimed. The inherited future outcome contract still uses quantity-weighted assigned-quantity denominators and `action_execution_selective_v3`: closed-campaign value is primary, full-panel continuous MTM is the accounting check, and UTC day-end inventory/open MTM/censoring are diagnostic-only. No prediction, action, Validation, holdout, or live permission has been created.

The execution-only [`carryover-safe v2 successor`](docs/causal_v12_ranked_toxicity_exposure_guard_carryover_safe_v2_implementation_20260803.md) now assigns an arm to an episode that survives inventory-campaign boundaries until every owned exchange order and guard state has naturally washed out. A real `2026-04-17` smoke consumed all 29,072 untreated quote decisions and completed 336 of 337 BUY and SELL episodes, with the final episode on each side right-censored. It recorded zero cross-arm ownership, forced washout cancels, owner mismatches, terminal hazard reuse, or cursor reuse.

That smoke also corrected the immediate implementation blocker: a still-active reducing SELL can become exposure-increasing when inventory changes. The same exchange order, queue position, and assignment ownership are retained while it remains in the real fill-risk set; one such transition was observed. Unknown, terminal, or non-fill-risk order identities still fail closed. The formal 40-day mechanics panel has not run, no economic result was read, and action, Validation, holdout, and live permissions remain false.

The execution linkage is additionally frozen by [`registration_linkage_v2`](docs/causal_v12_ranked_toxicity_exposure_guard_registration_linkage_v2_amendment_20260802.json). It verifies that the current operational config's `ml.model_dir/bundle_meta.json`, the registered v12 bundle artifact, and both side-specific frozen specs resolve to the same file and SHA256. The earlier config hash remains historical inside the immutable v1 specs; this amendment binds the current execution config without reinterpreting those bytes. Its machine audit passes, reads no outcomes, and grants no action or live authority.

`audit/` contains action panels, randomized assignments, CATE/OPE evaluators, repair paths, and policy artifacts. Shared dependencies: D, R, S, G.
