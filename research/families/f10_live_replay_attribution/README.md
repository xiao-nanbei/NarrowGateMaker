# F10 Live/Replay Attribution

Last materially modified: 2026-08-20

Documentation boundary: this README and the unit's tracked `docs/` are public. Owner-only artifact locators, unpublished evidence indexes, and private research context are resolved through this unit's ignored local `private/` catalog and are not distributed with the public repository. See the [public/private research layout](../../PRIVATE_EVIDENCE.md).

Status: active diagnostic and mechanism-calibration line. Its outputs can find state-machine, unit, gate-order, campaign, and loss-attribution defects but do not independently establish alpha.

The current live identity is [`operational_baseline_identity_20260820_v12`](docs/operational_baseline_identity_20260820_v12.json), resolved through [`operational_baseline_current.json`](docs/operational_baseline_current.json). v12 is a host-only successor to immutable v11: it preserves causal-v12 semantics-v6 ML ON, q90 action OFF, BUY fill-selection action/shadow OFF, and the active SELL Boolean cooldown policy `causal_multichannel_window_boolean_cooldown_owner_policy_v1`. That policy entered in v11 through an explicit `owner_risk_accepted_promotion`: the 50-day and 71-day point estimates were favorable, but the frozen research hard gates did not pass. BUY remains on the predecessor cooldown and reducing quotes are unchanged. The exact boundary is in the F05 [`owner active release`](../f05_fill_quality_quote_ev/docs/causal_multichannel_window_boolean_cooldown_owner_active_release_v1_20260812.md); the host-only boundary is in the system-engineering [`AWS migration v2`](../../system_engineering/docs/aws_tokyo_live_host_migration_v2_20260820.md).

The predecessor 50-day replay control is [`current_live_held_global_ber_control`](docs/current_live_held_ber_replay_baseline_50d_20260810.json). The current backtest control for new experiments remains the same active SELL owner policy now named by v12; the host migration does not create a new replay treatment. The predecessor panel remains the compatibility comparator used by the frozen F05 experiment. Its daily-fresh-start Development result is `-165.566079 USDC` terminal MTM, `-168.530979 USDC` closed-campaign value, and `20,147` fills. The F05 candidate improved that 50-day terminal result by `+11.372165 USDC` and the corrected 71-day restart-aware result by `+16.877254 USDC`. Both identities remain modeled-queue diagnostics without strict historical queue authority. The required strict raw-native Python replay with frozen Tokyo latency calibration remains a separate successor; see [`current_live_held_ber_replay_baseline_50d_execution_scope_amendment_v1`](docs/current_live_held_ber_replay_baseline_50d_execution_scope_amendment_v1_20260810.md).

The latest quote-coordinate diagnostic is [`paired_live_quote_coordinate_asymmetry_audit_v1`](docs/paired_live_quote_coordinate_asymmetry_audit_v1_20260801.md). Across 53,137 flat common-support BUY/SELL decision pairs, final SELL targets were 0.01448 bps farther than BUY on a pair-weighted basis; the day-clustered interval crosses zero. The observed quote surface therefore does not support a structural SELL-too-close explanation for the 360-hour loss.

The audit also withdraws the old `BUY 2.94 vs SELL 2.54 bps` opener entry-edge comparison. Thirty-one opener fills lacked exact lifecycle identity and mixed a fallback mid into the same field; after requiring finite lifecycle/order-age identity, BUY and SELL entry edges are 2.79772 and 2.80697 bps. A fresh 10-second sensitivity still finds negative maker value on both sides, but with a much smaller side gap. Campaign terminal PnL is unaffected by this fill-mid issue.

One independent engineering defect was confirmed: both markout EMAs are maker-signed, while the observed runtime inherited the historical `-1` side-asymmetry default. Python live, backtest, and C++ defaults are corrected to `+1`; the average coordinate impact is far too small to explain the loss. The explicit `+1` config was released separately as an engineering correction on 2026-08-01, without alpha, PnL, or closed-family authority.

Project routing keeps this engineering work separate from alpha discovery: q90 is `production_action_suspended_baseline_integrity_repair_active`, while the PnL route is `decision_visible_fill_quality_alpha_missing`. Only a replay claiming current q90-ON live equivalence must wait for the terminal-risk-set repair. New fill-quality feasibility may proceed with q90 frozen OFF identically in every arm, using exact lifecycle joins and no fallback mid. The q90 repair itself may not read economic outcomes or tune the policy threshold.

The latest baseline-integrity identity is [`buy_q90_fresh_prospective_placement_recovery_v4`](docs/buy_q90_fresh_prospective_placement_recovery_v4_implementation_20260802.md). It accepts only `PROSPECTIVE_CANCEL_REENTRY` after cancel ACK with positive remaining quantity, then evaluates a new BUY candidate at the current price, age zero, current causally visible book, fresh queue-at-tail, and current GTX activation support. The retired order ID remains only as lifecycle identity; its queue, elapsed path, hazard state, and depth cursor are forbidden inputs. Full fill cannot re-enter, reject/expiry returns to baseline resubmit, shutdown does not re-enter, and unknown terminal routes fail fast. Python and the pybind q90 kernel now share native ABI `dynamic_fill_hazard_native_book_q90.v4` for this prospective observation, without claiming full C++ tick-replay authority.

The preceding [`buy_q90_dual_clock_terminal_routing_contract_v2`](docs/buy_q90_dual_clock_terminal_routing_contract_v2_implementation_20260802.md) established the lifecycle and exposure foundation. The shared lifecycle now distinguishes `SUBMITTED`, `ACTIVE`, `PARTIALLY_FILLED`, `CANCEL_PENDING`, `EXCHANGE_TERMINAL`, `POST_CANCEL_RECOVERY`, and `REENTRY_ELIGIBLE`. Only the three live exchange states belong to the fill-risk set. It now reports separate exchange-clock and strategy-visible quantity-weighted exposure in BTC*s. The historical `quantity_time_exposure_btc_s` field is explicitly a compatibility alias for visible exposure; missing exchange timestamps yield an invalid physical estimand rather than silently borrowing the visibility clock.

Those fields now persist through the shared `order_lifecycle_journal.v1` in live and Python q90 replay, including validity, completeness, invalid reason, remaining quantity, and terminal route. The pybind q90 kernel still has no quantity-time exposure output: three-runtime parity applies to terminal routing/path/score/transitions, not to either (E_q) estimand.

Terminal policy routing is now reason-specific. Only cancel ACK with positive remaining quantity enters prospective re-entry recovery. Full fill ends the exposure action, reject/expiry returns to ordinary baseline resubmission, shutdown cannot re-enter, and an unknown reason fails closed. Every route removes the old exact-level cursor and makes old-order fill risk zero.

Together, v2 and v4 close the known terminal-order liveness trap and implement the fresh recovery transition in Python live, Python replay, and the pybind native q90 kernel without authorizing the action. The 40-day event lockstep and CIF mechanics have passed; the first same-date live/AWS transport has failed closed. q90 action therefore remains suspended, shadow collection continues, and F07 requires a new fully bound transport epoch before economics.

The preceding implementation identity [`buy_q90_terminal_recovery_contract_v1`](docs/buy_q90_terminal_recovery_contract_v1_implementation_20260802.md) remains frozen. v2 corrects its clock naming and terminal-route scope through a new implementation identity rather than rewriting that historical record.

The preceding completed mechanics identity is [`buy_q90_terminal_hold_riskset_audit_v1`](docs/buy_q90_terminal_hold_riskset_audit_v1_development_20260801.md). It confirms that the remaining q90 transport blocker is not primarily a deep snapshot-capacity problem. All 23 implicated BUY orders were exchange-terminal after cancel ACK, yet the retained tracker continued to synthesize them as `PENDING_CANCEL`. This produced 69,074 post-ACK active-order hazard evaluations: 62,495 invalid and 6,579 apparently valid. The 18 apparent recoveries and re-entries also consumed this invalid terminal-order estimand.

Its frozen decision remains `terminal_active_riskset_contract_failed_post_cancel_recovery_undefined` for the historical implementation it audited. The repair above does not rewrite that evidence. No timeout, depth expansion, threshold change, F07 v2, economic read, or live authority is created. The original valid-fraction and cancel-role gates remain unchanged and unevaluated under the repaired implementation.

The preceding mechanics identity is [`buy_q90_causal_visibility_clock_parity_v1_1`](docs/buy_q90_causal_visibility_clock_parity_v1_1_development_20260731.md). It repairs the exchange-truth/feature-ready clock split and removes the old q90 under-treatment: corrected replay produced 192 applied cancels on 2026-07-25 versus 184 in the observed live slice, with zero future-feature rows, unchanged truth/queue/fills, and zero Python/C++ lifecycle mismatches.

The identity nevertheless failed its frozen same-date live parity contract. Only 23 retained canceled orders generated 62,495 repeated `hold_invalid` rows after leaving the visible deep range, and cancel-role total variation was 0.151495 versus the frozen 0.15 limit. The same-day AWS capture also lacks deep-diff recording, so its latency profile is sensitivity evidence rather than an exact q90 event path. F07 v2 registration remains blocked; no economic or later-panel output was read.

The latest completed mechanics identity is [`buy_q90_live_action_rate_transport_parity_v1`](docs/buy_q90_live_action_rate_transport_parity_v1_20260731.md). It localizes the live/replay action-rate gap to a mixed historical visibility clock. Replay advances q90 on exchange time while comparing CryptoHFT provider receive timestamps with that earlier clock, which marks normal transport latency as `future_feature_time` and reduces the 2026-07-25 valid-evaluation rate to 0.4785%, versus 86.9979% in live. Exact-level activation support was 99.9585%, native sequence failures were zero, and conditional cancel/valid rates were similar. The engineerable defect is therefore historical replay under-treatment, not a proven duplicate-cancel or excessive-evaluation defect in live.

The old q90 ON/OFF result is still frozen evidence for the weak treatment it actually ran, but it is not transport evidence for the current live action intensity. A new coherent visibility-clock replay must be registered and run before q90 can be retained, closed, or adjusted on economic grounds. All policy, rollback, Validation, holdout, and live permissions remain false.

The latest completed identity is [`buy_q90_portfolio_path_attribution_v1`](docs/buy_q90_portfolio_path_attribution_v1_development_20260731.md), a 40-day Development-only same-market-path q90 ON/OFF full replay. On the 24 Grade-A primary days, q90 increased SELL-minus-BUY exposure-fill imbalance, but BUY suppression, SHORT-share, multi-level-SHORT-share and terminal-harm gates did not all pass. The frozen decision is `q90_portfolio_bias_mechanism_not_supported_development`.

This does not validate or exonerate the current live policy. Historical replay produced only 105 q90 cancels over about 960 hours, while the separate 120-hour live diagnostic recorded 2,501. This roughly 190-fold contextual action-rate gap leaves live transport unsupported. Validation, holdout, action, live and automatic rollback permissions remain false.

The completed Development-only [`first_add_decision_to_terminal_loss_diagnostic_v1`](docs/first_add_decision_to_terminal_loss_diagnostic_v1_development_20260729.md) uses the 24 Grade-A days from the existing F09 Development identity as its primary denominator; the 16 Grade-B gap-censored days remain sensitivity only. It requires a native exact decision -> order -> fill -> campaign join with 100% coverage and rejects nearest-time or repaired historical matches.

Its target is direct post-decision campaign value in USDC: `campaign_terminal_equity_usdc - decision_equity_usdc`. Across 2,071 first-add decisions, mean value was negative for BUY and SELL in both quality panels. The primary Grade-A means were -0.04272 USDC for BUY and -0.05564 USDC for SELL, with both clustered intervals below zero. However, decision-visible mechanism contrasts did not replicate consistently between Grade A and Grade B. The result establishes a loss denominator for F05, not an actionable state, and cannot create an F09 action or read later panels.

Local feature search is paused. F04's `first_add_external_incremental_value_m0_m1_v1` has completed its 30-distinct-day count gate, but it may use this same direct-USDC target and AWS receive/feature-ready clocks only after the corrected exact-lifecycle successor and every frozen chronological, side-specific, source-transport, common-row, true-LOO, causal-clock, and late-panel gate pass. In parallel, outcome-blind external quote mechanics may proceed now. This F10 target transports only to an exact first-add surface; an external guard covering all exposure-increasing quotes needs a newly frozen exact opportunity/value denominator. Existing future-direction labels or old maker-lifecycle targets cannot replace either contract.

The first outcome-blind E6/P2 run is complete on 16 historical quote-log days. It observed 26 outward quote changes, but only six were add opportunities; the legacy clock, two missing quote-log days, and absent order/ACK/queue journal keep that surface transport-unsupported. This does not alter the F10 first-add target or authorize an external action.

The latest live observational identity is [`live_120h_adverse_selection_diagnostic_v1`](docs/live_120h_adverse_selection_diagnostic_v1_20260730.md). Across 2,417 usable approximate 10-second fill observations, positive maker entry edge was outweighed by the subsequent adverse move; SELL opener and SELL add were weaker than BUY opener. SHORT and multi-inventory campaigns concentrated the net negative campaign PnL. The identity is hash-bound and recomputable from persisted CSVs, but its original ad-hoc generator identity is unavailable, the horizon has a median 2.072-second observation delay, and it remains diagnostic-only.

The non-frozen [`interpretation errata`](docs/live_120h_adverse_selection_diagnostic_v1_errata_20260730.md) clarifies that 74.7% is a share of the net negative aggregate, not gross loss; the multi-inventory gross-loss share is 46.1%, while SHORT campaigns account for 82.2% of gross loss. It also limits the cooldown/restart/fee statement to the price-markout identity and records that the historical `opener` label is a fill-time role rather than an exact submit-time opener identity.

This live evidence motivates two separate questions without authorizing either: SELL opener/first-fill conditional value belongs to F05, while cumulative post-fill SHORT inventory leverage belongs to F09. Order-age correlation alone does not reopen F07 keep/cancel.

The larger diagnostic-only [`live_240h_loss_solution_routing_v1`](docs/live_240h_loss_solution_routing_v1_20260801.md) now sharpens that split. Across 1,951 clean closed campaigns, single-level campaigns contributed only -0.0786 USDC, while multi-level campaigns contributed -13.4926 USDC. Single-level SHORT was +0.0921 USDC; 257 multi-level SHORT campaigns were -11.1471 USDC. Because inventory depth is endogenous, this is mechanism localization rather than proof that blocking an add improves PnL. The later F09 cumulative-inventory-budget and passive-repair actions both closed on Development, so this paragraph no longer routes a successor action. It remains motivation for exact value attribution without reopening SELL opener, order-age, cooldown-clock or inventory-control actions.

The first question has now been evaluated under the corrected [`sell_first_fill_conditional_value_feasibility_v3`](../f05_fill_quality_quote_ev/docs/sell_first_fill_conditional_value_feasibility_v3_development_20260730.md) identity. Its F10 native producer completed 33/33 formal Development days and 12,025 exact first-opener lifecycle rows. Grade-A and Grade-B SELL averages were negative, confirming the aggregate loss denominator, but the submit-time local model failed prediction and selective-value gates. This closes the local conditional-selection branch; it does not authorize suppressing all SELL openers or any live policy change.

The runtime-startup authority foundation remains [`buy_q90_runtime_authority_contract_v3`](docs/buy_q90_runtime_authority_contract_v3_implementation_20260802.md). It makes the q90 action suspension a runtime invariant across direct startup, `run.sh start/restart`, deploy preflight, and SIGHUP. An unsupported q90 action cannot start without an explicit owner override; any effective override is logged and persisted in the atomic runtime identity. The v4 successor now adds fresh prospective placement recovery as described above. The repaired mechanics are represented in replay and prospective lifecycle infrastructure, but the failed live/AWS transport grants no q90 economic, action, or live authority.

`audit/` owns reports, lifecycle attribution, metrics, and campaign mechanism diagnostics. Shared dependencies: R, S.
