# F03 因果 13-head 模型

[English](README.md) | [简体中文](README.zh-CN.md)

Last materially modified: 2026-09-13

Last materially synchronized: 2026-09-13

文档边界：本 README 与该单元受版本控制的 `docs/` 属于公开文档。仅限所有者的产物定位、未发布证据索引和私有研究上下文，通过该单元被 Git 忽略的本地 `private/` 目录解析，不随公共仓库分发。参见[公私研究布局](../../PRIVATE_EVIDENCE.md)。

状态：历史 causal-v12 semantics-v6 模型及冻结的回放／运行对照组件。公开代码树说明特征、标签、训练和回放机制，但不标识当前部署的模型、策略、版本或动作状态。

1 秒频率的后续模型已经完成冻结的 40 日原生数据 Development A/B，并已关闭。相对 v9 的 10 秒对照，终点市值计价损益变化为 `-2.116974 USDC`，保留 87.02% 的成交，库存时间增加 888.737 BTC*s，q10/CVaR 恶化。未打开 Validation 或连续 71 日确认。参见[该次历史报告](docs/causal_v12_1s_native_40day_full_path_ml_ab_v3_development_20260808.md)。

本单元根目录负责训练、特征配置实验和主动成交节奏特征。模型运行合同仍归 `strategy/` 所有，因为它们是 live/replay 共享语义。共享依赖为 D、R、S、G。

具体部署与回测身份属于私有证据，状态为 `private_not_distributed`。公开家族文档不授予 live、动作、动作发生或经济结论的权威性。私有调用方必须核验自己绑定的实际文件，失败时拒绝使用；部署配置别名不能代替回测身份。

历史拟合后证据位于 `docs/causal_v12_postfit_native_oos_20260726_31_20260802.md`。五个 Grade-A 原生数据日期产生了正的 ML-ON 终点损益点估计，但预测家族仅通过 5/13 个 head 检查，完整路径筛查未通过损益不确定性、成交保留、campaign 尾部和 SELL maker 价值检查。

## 已退役 causal-v2 特征：比较失真／仅供历史参考

2026-09-13，所有者授权删除 `${NARROWGATE_DATA_ROOT}/features_btcusdc_causal_v2`。这是特征／数据集目录，不是模型权重。其中 123 个日级特征文件、4 个合并数据集及特征清单已删除；历史文档仅留作参考，不再是可以直接运行的当前输入说明。禁止将此目录恢复为默认输入、用其他数据冒充其身份，或将其中的旧训练／验证／测试数据集混入新一轮训练。

“比较失真”表示这些特征及其历史诊断不能代表当前数据和模型合同。已核实的原因与边界如下：

- 2026-07-15 清单登记 119 个日文件，目录实际有 123 个。[2026-07-20 对齐审查](../../../docs/marketdata_good_day_alignment_20260720.md#frozen-features)已指出 2026-07-12 至 2026-07-15 四个未登记日期。因此，遍历文件夹与按清单读取会得到不同样本。
- 清单采用 `chronological_good_day_with_embargo`，不是当前事先确定的完整日历；此前日期缺失会终止因果预热。旧的日期选择和预热支持不能被表述为重建后日历的覆盖情况。
- 它采用 10 秒桶结束后可用的特征与第 2 版标签语义：收益／方向结果从窗口 `h` 内的成交延伸至成交后 `h` 的 markout，可能到决策后 `2h` 才结束。旧数据集不能与重建的 Bar／特征及重新生成的标签互换。这是语义和支持范围不匹配，不是证明每个历史标签都有未来泄漏。
- [7 月清理重训报告](docs/model_runtime_cleanup_retrain_20260717.md)和[后续时间／单位修复](../../system_engineering/docs/time_unit_contract_repair_20260726.md)记录了取代它的模型／特征合同。旧候选指标只具有历史意义。causal-v3 已记录的 metrics 提前五分钟暴露问题，以及后续日历特征问题，不能未经逐行审查就归为所有 causal-v2 行的已证实缺陷；本次清理没有进行这种逐行审查。

上述两份 7 月报告通过本维护索引明确标为**历史参考／用于当前研究时比较失真**，不将原实验记录改写为新测试。私有定位条目记录本次删除，不再可用于复现。previous-use 和锁定研究边界不变。购买的原始数据、当前重建输入、其他特征目录、模型权重和 live 状态均未改动。本次清理不代表替代特征已经生成，也不代表新模型已经验证。
