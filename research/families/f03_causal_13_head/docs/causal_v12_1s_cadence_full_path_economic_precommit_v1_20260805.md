# F03 One-Second Cadence Full-Path Economic Precommit v1

Last materially modified: 2026-08-05

Status: frozen before candidate training, prediction reads, or economic reads.

This identity compares the true one-second 13-head successor against the current v9 ten-second ML-ON operational baseline. It does not compare against ML-OFF and it does not change P3, quote formulas, requote, cancel, cooldown, inventory, or order-size policy.

The first economic read uses the frozen 40 native Development days. The exact ordered list must equal the bound source profile's `development_40.days`; a separately typed list in this precommit is not sufficient. Those days were used by earlier F03 hypotheses, so they cannot create independent confirmation. They can support the permanently labelled owner progression route only after the exact candidate bundle, DAG, runner, C++ ABI, data, and latency identities are bound in an execution amendment.

No independent Validation or family-specific sealed holdout is registered in this identity. The existing 2026 native panels were already read by earlier F03 hypotheses and cannot be repackaged as independent confirmation. They may not be attached after the Development result is known. A future normal research-supported route therefore requires a new successor precommit that freezes its own unread confirmation panels before any successor outcome read. The present identity can only support the permanently labelled owner route.

The continuous sensitivity is the exact ordered 71-day UTC calendar from 2026-04-17 through 2026-06-26, bound to the frozen calendar-continuity manifest. The current precommit does not claim that a continuous candidate runner is already bound or eligible: a later execution amendment must admit all 71 trading days, bind the exact restart schedule, and permit no whole-day placeholders before any continuous-path PnL is read.

Direct live canary is not granted by a positive point estimate. The normal hard-gate route retains the exact `action_alpha_v2` minimum fill retention of 85%. The owner route precommits a disclosed 80%-120% fill band, but a result below 85% remains an `action_alpha_v2` hard failure with `ranking_score=null`; it cannot be relabelled as a research-supported pass. The owner route also requires positive day-clustered lower bounds for native closed-campaign value and terminal MTM, non-worsening campaign q10/CVaR, MAE, maximum inventory, inventory time, side maker value, and multi-level LONG/SHORT loss. It also requires the 71-day restart-aware continuous-calendar sensitivity, Python/C++ parity, runtime p99, preflight, and rollback gates.

UTC day-end inventory, open-campaign MTM, and censoring remain diagnostics with zero ranking weight. Cash, inventory, campaign state, and final inventory MTM must be accounted continuously in the 71-day confirmation. The explicitly labelled owner path can lead directly to a bounded 10% active canary after all of its economic, parity, runtime, and rollback gates pass; no long observation-only shadow is required. A normal hard-gate promotion belongs to a separately frozen successor identity because this identity has no independent confirmation panel.

The current v9 pointer, v9 identity, private configuration, dual-path governance contract, exact 40-day source profile, 71-day continuity manifest, score implementation, and exact `action_alpha_v2` payload are all hash-bound. Every runtime item is phrased as a requirement, not as evidence that parity, preflight, p99, or rollback has already passed.

The machine-readable contract is `causal_v12_1s_cadence_full_path_economic_precommit_v1_20260805.json`.
