# Tardis 迁移审计修复进度

[English](tardis_migration_audit_followup.md) | [简体中文](tardis_migration_audit_followup.zh-CN.md)

Last materially modified: 2026-09-22

Last materially synchronized: 2026-09-22

本文跟进用户提供的 `09c13a98` 静态审计，不代表全部研究族或经济回测已经验收。原审计文档保留在用户本地，不随本文分发。历史机制仍可研究；换源既不验证旧阳性结果，也不否定全部旧阴性结论。

## 后续 dbd8588 审计：2026-09-22 跟进

用户后来提供的全研究族审计与 09c13a98 使用不同的 D 编号含义，不能混用。该计划尚未全部关闭。

| dbd8588 项 | 当前状态 |
| --- | --- |
| D01 日历准入 | 消费者现拒绝中间缺日、重复、乱序和缺少必需市场／通道；仍允许选择合法子区间。新增五个合成案例覆盖边界和路径迁移。 |
| D02 混装 head 身份 | 发布器／加载器绑定逐头来源与哈希；新冻结 F03 包与历史模型分开。 |
| D03 未知毒性结果 | 非有限未来价格保持未知标签和无效 outcome end；保留回归覆盖。 |
| D04 个体与包计数 | 新可选 live 协议使用原生个体成交；实际部署验收独立且仍待完成。 |
| D05 F05 补零 | 新 public-input 路径独立；历史 cleaner 仍补零，不得把旧入口当新协议训练准入。 |
| D06 隐式标签流动性分支 | 仍需显式后继策略与验收；不能为隐藏此限制而静默重生成已冻结 F03 标签／模型。 |
| D07 峰值内存 | 已有分批读取，但回放仍物化数组，不宣称普遍有界 RSS。 |

F03 已有真实新训练和经济证据，见[当前报告](../research/families/f03_causal_13_head/README.zh-CN.md)，但不代表其他研究族实验完成。原生消息／队列一致性、全日历及跨日验收、matcher 不重复消费、checkpoint 一致性及原审计 U 项证据必须保持各自实际状态，合成测试不能替代真实验收。

本次还恢复了回放便捷入口的提前拒绝参考市场检查：prepare-once 重构曾把检查移到输入加载之后，现在回归测试再次确认加载前拒绝。

## 首批已核实修复

| 审计项 | 当前处理 | 证据边界 |
| --- | --- | --- |
| D01：空成交文件清掉 ID 防线 | 保留所选市场最近非空 ID 高水位；中间空文件不再放过未经核销的重叠 | 合成重叠、非重叠及其他市场隔离测试；不宣称新增全局去重 |
| D05：静默关闭参考市场 | 加载执行侧输入前，拒绝显式启用参考市场的请求 | 拒绝测试及既有执行侧消费者测试；未新增 F04 适配器 |
| Q04：未知毒性结果变成 0 | 非有限结果价格保持 NaN，且不生成有效 outcome end | 双侧覆盖 NaN、正负无穷；真实有限价格的 0 标签仍有效 |

这些修复不更改实盘配置、不启动研究运行、不覆盖已有产物。测试使用受控样本，不含采购行情。正在运行的任务保留原源码身份；发布新代码不等于追溯验收其输出。

## 校准与模型整包绑定

D02 已在 P3 拟合、标签生成和训练身份读取入口校验：训练日期、包含前日上下文的源清单身份、交付参数、市场、触达期限、距离单位和报价 tick 必须一致。实测延迟文件迁移路径不改变内容哈希。P3 的一秒特征定时器与面板的十秒定时器允许不同；行情发布频率、延迟、处理耗时、相位、陈旧阈值、随机种子和同时间排序不在豁免范围。现有日回执已包含新版拟合器需要的证据，不因新增校验自动重算。

D03 已在发布和模型加载入口校验。十三份元数据必须各自对应正确的头，并与指定训练合同具有一致的输入、标签、划分、来源父身份、训练内选择规范和权重策略。逐头 outcome 目标和有效行数允许不同。所有头通过后才原子发布模型根清单，同时绑定模型和元数据字节，加载时再次验证。缺少元数据的旧模型不会被升级为兼容整包。反例测试覆盖不同半衰期、选择规范、来源、标签合同，以及修改元数据并同步更新文件哈希的混包。

## 已确认但尚待完成

D04 已改为默认严格加载：F05 要求来源、训练、标签身份、有序 booster 特征列及拒绝或保留 NaN 的缺失语义。旧默认行为须使用 `load_legacy()`，历史 shadow 评估器也须显式选择 legacy。这不代表机会特征生成已经迁移，也没有训练新 F05 模型。各族边界和待实现适配器见[研究输入迁移](../research/INPUT_MIGRATION.zh-CN.md)。

本补丁不验收审计中的 B01 会计接入、B02 五个直接经济输出、Q01 供应商分组原子性、Q02 事件计数等价、Q03 生产语义身份、Q05 完整订单生命周期。未知资金费或估值保持未知；不能为声称十三头全部被使用而强塞报价通道。历史入口及研究族、博客适用范围仍需逐项跟进，不批量改写冻结结论。

后续 F03 优先在新校准、标签和模型校验基础上接通执行与会计链。当前实验计划仍是执行依据；本审计不恢复旧 B0、ML-OFF 或参考市场要求。
